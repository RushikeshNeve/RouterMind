# RouteMind Dashboard

Next.js developer dashboard for RouteMind AI Gateway operations.

```bash
npm run dev -w @routemind/dashboard
```

Set the API base URL with:

```env
NEXT_PUBLIC_ROUTEMIND_API_URL=http://localhost:3000
```

Local URL: `http://localhost:3001/dashboard`.
