# RouteMind: Project → Product

### A phase-wise plan combining engineering, pricing, and go-to-market — July 2026

This plan ties together the three prior conversations: the architecture review, the v2.0/v3.0 technical roadmap, and the market positioning research. Nothing here contradicts those documents — this is the sequencing layer that says _when_ each piece happens and _why that order_, with engineering, monetization, and trust-building treated as one track instead of three separate workstreams.

---

## The honest starting position

**What you have**: a technically sound single-tenant LLM gateway with a genuinely differentiated routing engine and prompt firewall.

**What you don't have yet**: anyone who can pay you, anyone whose production traffic depends on you, and a reason a stranger would pick you over Portkey, LiteLLM, or OpenRouter without you explaining it to them personally.

**What "product" means concretely**: a stranger can discover RouteMind, understand what it's for in one sentence, sign up, hit their first budget/RBAC/routing constraint, pay for it, and trust it enough to route real traffic — all without talking to you. Every phase below exists to make one more piece of that sentence true.

**Current phase:** Phase 2 — Trust & Differentiation (see `docs/phase-2-plan.md`). Phase 1's checklist (all 10 items) plus its exit-criteria test are complete (see below); Phase 0's Ship list was complete before that.

---

## Phase 0 — Foundation Hardening

**Theme: make the thing you already built trustworthy for more than one tenant**
**Timeframe: ~6–8 weeks | Engineering-heavy, no GTM yet**

This is the tenancy/RBAC/policy-engine work from the architecture review, unchanged — it's the load-bearing wall everything else stands on, so it goes first regardless of which USP you eventually lead with.

**Ship — all complete:**

- `Organization → Workspace → Membership → Principal` schema migration — **done**: additive → backfill → FK-tightening slices landed across 2026-07-10, applied to production, verified via a real backfilled 1:1:1:1 result for existing users (see session log).
- RBAC (Owner/Admin/Developer/Viewer) — **done**: seeded role→permission taxonomy, `requirePermission`/`requireOrganizationPermission` middleware, enforced across every workspace-scoped mutating route, full role×route matrix tested (see session log).
- Service accounts as first-class principals — **done**: create + issue keys + list + key revocation, dashboard UI, RBAC-parity verified (see session log). Deleting the service account itself (not just revoking its keys) was never asked for and isn't built.
- BYO Router Model (customer-owned routing credentials, resolution order: request → API key → workspace → org → platform default) — **core mechanism done**: schema, resolver, identity plumbing, live wiring into `LiveRouterLLMService`, `configSource` response-metadata surfacing, workspace/org CRUD routes, and a workspace-scoped dashboard UI all shipped and verified end-to-end via `phase0-isolation.test.ts` (each workspace's own distinct `RouterConfig` proven isolated). Four sub-items intentionally deferred — see "Deferred, not blocking" below.
- Declarative policy engine (budgets + model restrictions as rows, not conditionals) — **fully done**: schema, `evaluatePolicy()`, live wiring for all three rule types (`budget`, `model_restriction`, `cost_cap`), CRUD routes, and a `/dashboard/policies` screen. Each rule type proven to actually block a real `/v1/chat/completions` request with a distinct, correctly-labeled error code (`BUDGET_EXCEEDED`/`MODEL_RESTRICTED`/`COST_CAP_EXCEEDED`), and proven workspace-isolated (a blocking rule on one workspace never leaks into blocking another's identical request) via `phase0-isolation.test.ts`. The legacy user-only (no workspace) threshold check is intentionally unchanged — `Policy.workspaceId` is required and has no pre-tenancy equivalent.
- Audit log — **done**: the 7 RBAC-gated workspace/service-account routes, `onboarding.ts`'s full mutating surface (closing a real IDOR in the process, not just an audit gap), and workspace creation itself are all audited, same-transaction guarantee verified throughout (see session log).

**Deferred, not blocking** (explicitly scoped out of Phase 0, all four are BYO Router Model sub-items):

- **api_key-scoped `RouterConfig` CRUD** — workspace- and org-scoped CRUD exist; per-API-key scoping was deferred since no caller has asked for it yet.
- **An org-level dashboard UI for `RouterConfig`** — blocked on org-context infrastructure (an org switcher, `GET /v1/organizations/:id/me`) that doesn't exist anywhere in the dashboard yet; building it standalone for one screen would be premature.
- **`configSource` visibility in a real Requests view** — the field already exists on every routing decision, but the dashboard's Requests page is still a demo-data stub with zero real backend wiring; surfacing `configSource` there is gated on that page becoming real first.
- **A per-request override field** for `resolveRouterConfig()`'s "request" scope level — the resolver already supports it and it's covered by unit tests, but no `/v1/chat/completions` request field exists yet to actually trigger it live.

**Explicitly don't do yet:** dashboard polish, marketing site, pricing page, MCP gateway, observability stack. All of it is wasted if the schema underneath changes shape.

**Exit criteria:** you can create two separate fake companies in your own dev environment, each with two workspaces, different roles, different router-model credentials, and prove one workspace's admin cannot see or spend the other's budget. If you can't demo that convincingly to yourself, you're not done. — **Verified 2026-07-10** by a real integration test (`apps/api/src/routes/phase0-isolation.test.ts`), not a manual check; see session log. **Re-verified and extended 2026-07-21**: the same test now also proves isolation for each workspace's distinct `RouterConfig` and for all three live `Policy` rule types (`budget`, `model_restriction`, `cost_cap`), not just budget, via a real `apps/api` suite run (251/251 passing) — see session log. **Phase 0 is complete**: every Ship item above is live-wired and exit-criteria-verified; the four deferred BYO Router Model sub-items are non-blocking polish, not foundational gaps.

**Known issues (found by the 2026-07-12 manual QA pass):**

- ~~`POST /v1/workspaces` has no permission gate at all and defaults `ownerUserId` to the literal string `"dev-user"`...~~ — **fixed 2026-07-12**, see session log.
- ~~No self-serve sign-up exists anywhere...~~ — **fixed 2026-07-12**, see session log.
- ~~A normal (non-invite) login doesn't land the user in a default workspace...~~ — **fixed 2026-07-18**, see session log.

All three known issues from the 2026-07-12 manual QA pass are now closed — see the session log for detail on each.

**Why this order:** every later phase — billing, the BYO-Everything USP, enterprise trust — is unbuildable without this. It's also the least visible, least exciting phase, which is exactly why it has to be forced first, before the temptation to build visible features takes over.

### Security incident record — `onboarding.ts` IDOR (2026-07-07 → 2026-07-20)

**This is a security incident record, not a routine bug-fix log entry.** It documents a real, live vulnerability that existed in production, not a design gap caught before shipping.

**What the vulnerability was:** Insecure Direct Object Reference (IDOR / CWE-639). Five routes in `apps/api/src/routes/onboarding.ts` accepted a `userId` (or a bare credential `id`) directly in the request body/params/path and used it to authorize the mutation, with **no check that the caller controlled that account**:

- `POST /v1/api-keys` — mint an active API key for any existing `userId`.
- `POST /v1/provider-credentials` — attach/overwrite a provider credential on any existing `userId`.
- `PATCH /v1/provider-credentials/:id` — enable/disable any credential by guessing or observing its id; zero ownership check at all.
- `POST /v1/model-access` — grant or revoke model access for any existing `userId`.
- `GET /v1/users/:userId/available-models` — read any user's configured providers/models.

`userId`s and credential ids are cuids, not secrets, and are returned in plaintext by `POST /v1/users` and other responses — so an attacker who observed or was handed one `userId` (e.g. from a shared demo, a support ticket, a leaked log line, or simple enumeration if any id ever appeared in a public place) had full read/write control over that account's provider credentials and API keys, with no authentication of any kind. `POST /v1/users` and a user's very first `POST /v1/api-keys` are excluded from this finding — those are the intentional, true bootstrap steps with no prior credential to check against.

**How long it was live:** Confirmed via `git log` — `onboarding.ts` and its backing store were added in `8b20f30` ("feat:llm router initial changes"), dated **2026-07-07**, the second commit in this repository's history. The fix landed in `19cc726` on **2026-07-20** — a 13-day exposure window in the codebase. The exact date these routes first became reachable in **production** is not established with confidence: the roadmap's own 2026-07-10 session-log entry records that 13 of 14 migrations were _already_ applied to the Neon production database via "a deploy path outside this session," meaning the first real production deploy predates this project's own visibility into it and was never pinned to a date. Given the README has documented `https://routermind.onrender.com` as the live backend since early in the project, the safe assumption is these routes were reachable in production for most or all of the 13-day window, not only from 2026-07-10 onward.

**Fix:** `POST /v1/users` and a user's first `POST /v1/api-keys` remain unauthenticated (true bootstrap — no key exists yet). Every other mutation now requires the caller to present that same user's own API key via `X-API-Key`; the `userId` named in the request is checked against the authenticated caller and rejected (401/403) on any mismatch. `PATCH /v1/provider-credentials/:id` returns an identical 404 for "doesn't exist" and "isn't yours," so ownership can't be probed even post-fix. Full detail in the 2026-07-20 session-log entry below.

**Exploitation check — explicitly not confirmed clean, flagging rather than asserting:**

- **Application-level records were checked and found empty, not merely unavailable.** `onboarding.ts` never wrote to `RequestLogStore` — grep confirms the only three call sites of `requestLogStore.create(...)` in the entire codebase are inside `chat-completions.ts` (plus two unrelated test files). No `AuditEvent` rows exist for any pre-fix onboarding action — that absence is exactly the gap this incident's fix closes. So there is **no queryable application data, at any point before this fix, that could show who called these five routes, when, or with what `userId`.**
- **Render's platform-level access logs were not checked** — this session has no Render API/CLI credential and did not request one, given production credentials have already been pasted into this project's chat history three separate times (2026-07-10, 2026-07-11, 2026-07-18) and flagged for rotation each time without confirmation it happened. Adding a fourth credential-paste to investigate this incident was judged not worth repeating that pattern.
- **Recommended follow-up for whoever holds Render access** (not performed here): check Render's log retention window for the `routermind` service — most tiers retain only a handful of days, so if retention is short, evidence from as early as 2026-07-07 may already be gone by 2026-07-20 regardless of who looks. If any retained logs exist, look for `POST`/`PATCH` calls to the five routes above where the source IP or timing looks inconsistent with the account's own signup session.
- **Also recommended, independent of logs:** rotate the `CREDENTIAL_ENCRYPTION_KEY` and treat all existing `ProviderCredential` rows created before 2026-07-20 as provisionally suspect — not because tampering was found, but because the absence of any audit trail means tampering _could not have been ruled out_ for that period. This is a judgment call for whoever owns the production data, not a decision made unilaterally here.

**Bottom line:** the vulnerability is closed as of `19cc726`. Whether it was ever actually exploited is **unknown and, for the application-log side, unknowable** — the gap this incident describes is precisely why no record exists either way. This is stated plainly rather than assumed benign.

### Session log

- **2026-07-10**: Landed the first slice of the `Organization → Workspace → Membership → Principal` schema migration — added the `Organization`, `Principal`, and `Membership` models plus a nullable `Workspace.organizationId` FK (migration `20260710110625_add_org_principal_membership`). Purely additive: no backfill, no changes to the existing `userId`-keyed tables (`ApiKey`, `ProviderCredential`, `WorkspaceMember`, etc.) yet. **Decision**: shared types for these models (`packages/shared/src/types`) were deliberately deferred to the slice that first consumes them, rather than added speculatively now — flagged by `db-migration-check`, resolved as an explicit call, not an oversight. **Next**: backfill script (Principal per existing User, default Org/Workspace per user, reusing any workspace from the earlier beta) + the FK-tightening/cutover slices on `ApiKey`/`ProviderCredential`/etc., per the migration-planner's 4-migration plan (additive → backfill → tighten constraints → cutover). Schema migration checklist item stays unchecked — this is one slice of several, not the full item.
- **2026-07-10**: Added `apps/api/src/scripts/backfill-tenancy.ts` — idempotent backfill that creates a default Organization + Workspace + Owner Membership (+ Principal) per existing User, keyed by a deterministic `personal-<userId>` Workspace slug so re-runs and partial-run repairs are self-healing without needing a `User.principalId` FK yet. Does not touch `ApiKey`/`ProviderCredential`/`UserModelAccess.workspaceId` — that's still a later slice. **Gotcha**: this repo has no staging/production-like data tooling (no `.env.example`, no snapshot/restore workflow) — verification substituted local dev Postgres with synthetic multi-user seed data (full run, idempotent re-run, and a simulated partial-run repair), not an actual production-data dry run. Flagging that gap rather than claiming compliance with the original ask. **Next**: either build real staging-data tooling as its own slice, or proceed straight to the FK-tightening/cutover slices on `ApiKey`/`ProviderCredential`/`UserModelAccess`.
- **2026-07-10**: Added real (nullable, `onDelete: Restrict`) FK constraints from `ApiKey`/`ProviderCredential`/`UserModelAccess.workspaceId` → `Workspace` (migration `20260710121648_add_workspace_fk_apikey_credential_modelaccess`), and `apps/api/src/scripts/backfill-workspace-fk.ts` to populate them from each user's `personal-<userId>` workspace. Verified against local dev Postgres: 18 rows backfilled across 3 tables, idempotent re-run (0 updated), and an unresolved-row case (user with no personal workspace) correctly left `NULL` and reported instead of guessed. Full `apps/api` suite (94 tests) still green. **Decision — did NOT do the full ask**: the original task asked to also make `workspaceId` required and drop `userId` in this same slice. Audited every app-layer call site first and found that would break the app immediately, not eventually: `authenticator.ts` runs raw SQL that JOINs on `ApiKey.userId` (dropping it breaks auth for every request), `workspace-service.ts`'s raw INSERT explicitly writes `userId`, and `onboarding-store.ts`'s `createApiKey`/`createProviderCredential`/`upsertModelAccess` (the live `/v1/api-keys`, `/v1/provider-credentials`, `/v1/model-access` routes) never set `workspaceId` and depend on the `userId_provider(_model)` compound-unique Prisma keys for upserts — making `workspaceId` `NOT NULL` breaks those writes today, dropping `userId` breaks the TypeScript build. Declined, not silently skipped. **Next**: a dedicated app-layer rewrite slice (those four files) has to land before `workspaceId` can be made required or `userId` can be dropped — that's the real blocker on Migration C/D of the original 4-migration plan, not scheduling.
- **2026-07-10**: Added `ServiceAccount` (new, empty — no app code creates these yet), a nullable `User.principalId` FK, and a nullable `ApiKey.principalId` FK (migration `20260710124433_add_principal_fk_user_apikey_service_account`), plus `apps/api/src/scripts/backfill-principal-fk.ts`. The script reuses the Principal already created per-user by `backfill-tenancy.ts` (via the personal workspace's Owner Membership) rather than minting a duplicate — verified `principalCount` stayed equal to `userCount` after backfill. **Gotcha**: `prisma migrate dev` refused to run non-interactively for this migration (it wanted to confirm the new `User.principalId` unique constraint) — worked around by generating the SQL with `prisma migrate diff --script`, hand-placing it in a timestamped migration folder, and applying with `prisma migrate deploy`, then confirming sync with a no-op `migrate dev` dry run. **Decision — same as the previous slice, did NOT do the full ask**: declined to make `principalId` required or drop `ApiKey.userId`, for the identical reason (`authenticator.ts`/`workspace-service.ts`/`onboarding-store.ts` still exclusively use `userId`, confirmed unchanged). **Also this session**: a request to run this same cutover (including the `userId` drop) directly against **production** via `prisma migrate deploy` was declined — no migration dropping `userId` exists yet to deploy, and dropping it would break production the same way. The user then pasted live production Postgres (Neon) and Redis (Upstash) credentials directly into chat; both should be rotated since they're now in conversation history outside anyone's control, independent of anything else in this log. **Next**: the app-layer rewrite slice is now blocking three separate cutover items (`ApiKey`/`ProviderCredential`/`UserModelAccess.workspaceId`, `ApiKey.userId`→`principalId`, and the production deploy) — worth doing as its own dedicated slice before any more schema work in this area.
- **2026-07-10**: First real production deploy. `prisma migrate status` against the Neon production DB showed 13 of 14 local migrations already applied (a deploy path outside this session — worth understanding, not yet explained), with only the `ServiceAccount`/`principalId` migration pending. Deployed it with explicit confirmation, then discovered production has **real data** (1 User, 1 ApiKey, 1 ProviderCredential) that had never been backfilled — `Organization`/`Workspace`/`Principal`/`Membership` were all at 0. Ran all three backfill scripts (`backfill-tenancy`, `backfill-workspace-fk`, `backfill-principal-fk`) against production with confirmation, verified a clean 1:1:1:1 result for the one real user afterward by direct query. Nothing was made required or dropped, so this shouldn't affect whatever's currently deployed against that database. **Next**: rotate the Neon/Upstash credentials (still outstanding); understand the pre-existing prod deploy path before doing this manually again.
- **2026-07-10**: Added `Role` (seeded Owner/Admin/Developer/Viewer), `RolePermission` (role → permission string, 8-permission taxonomy from `architecture.md` line 114 — not the roadmap, minor correction on the source), and a nullable `Membership.roleId` FK alongside the existing `role` string column (migration `20260710131536_add_rbac_role_permission`), plus `apps/api/src/scripts/seed-rbac.ts` (idempotent, 4 roles/17 permission rows, verified no duplicates on re-run) and a standalone `requirePermission(perm)` Fastify preHandler in `apps/api/src/infrastructure/rbac.ts` with 6 unit tests, all passing. **Decision**: the role→permission matrix itself isn't specified anywhere in the docs — designed as a strict-superset hierarchy mirroring the existing legacy `WorkspaceRole` matrix in `workspace-service.ts`; worth a second look before this becomes load-bearing. **Per explicit scope**: not wired into any route, existing `Membership` rows (including the one in production) were not backfilled with `roleId`, and the separate legacy `WorkspaceRole`/`hasWorkspacePermission` system on `WorkspaceMember` was left untouched — two RBAC systems now coexist deliberately. **Checklist**: "RBAC (Owner/Admin/Developer/Viewer)" stays unchecked — this is the schema/middleware scaffolding, nothing enforces anything yet. **Next**: wire `requirePermission` into mutating routes, backfill `Membership.roleId` for existing rows (including production), and decide whether/how to retire the legacy `WorkspaceRole` system.
- **2026-07-10**: Wired `requirePermission` into the workspace-scoped mutating routes (`PATCH /v1/workspaces/:id`, `POST/PATCH/DELETE .../members`, `POST .../api-keys`) and enforced last-Owner protection (409 on demoting/removing a workspace's sole Owner). **Scope decision, made explicitly with the user before implementing**: the original ask was every mutating route including `onboarding.ts`'s `/v1/api-keys`/`/v1/provider-credentials`/`/v1/model-access` — exploration found those have zero auth today and gating them creates a bootstrap circularity (you'd need an API key to create your first one), so they're deliberately untouched; only the workspace-scoped surface is covered. Identity now comes from the caller's own API key (`request.rbacContext`, set by `requirePermission`), replacing the old `actorUserId`/`userId` body-param trust model — `assertPermission` and those schema fields are gone. **Bug found and fixed while wiring this in**: `requirePermission` only checks the caller's permission in their own workspace — nothing stopped a valid API key for workspace A from acting on workspace B. Added `ensureSameWorkspace()` to every gated handler; covered by a dedicated 403 test. `app.ts` now defaults to a real `PrismaClient` (previously fully DB-agnostic) since `requirePermission` needs direct Prisma access the `WorkspaceService` abstraction doesn't expose. Added `backfill-membership-role.ts` (maps existing `Membership.role` strings to `Role` rows — run locally, not yet run against production). New `workspaces-rbac.test.ts`: full 4-role x 5-route allow/deny matrix (20 cases) + 5 edge cases (no key, cross-workspace key, last-Owner demote/remove, demote-with-another-owner-present) — 125 tests total passing repo-wide. **Checklist**: "RBAC (Owner/Admin/Developer/Viewer)" still stays unchecked — real enforcement now exists for the workspace-scoped surface, but `onboarding.ts` remains fully open and the legacy `WorkspaceRole`/`hasWorkspacePermission` system on `WorkspaceMember` still coexists untouched. **Next**: production `Membership.roleId` backfill, a decision on `onboarding.ts`'s auth story, and retiring (or explicitly keeping) the legacy WorkspaceRole system.
- **2026-07-10**: Ran the production catch-up: `seed-rbac.ts` then `backfill-membership-role.ts` against the Neon DB — production had never had `Role`/`RolePermission` seeded at all (only local dev did), so the backfill initially found 0/1 resolved until roles were seeded, then 1/1. Production data is now consistent with what `requirePermission` expects. **Still unresolved**: whether the actual running production server has picked up this session's application code at all — there's no observed CI/CD in this repo, `git push` alone doesn't redeploy it, and how `apps/api` actually gets deployed to production is unknown to this session. Data is ready; code readiness is unverified.
- **2026-07-10**: Resolved the deploy question above — `apps/api` is deployed on Render (dashboard/website are separate Vercel deployments). The deploy for commit `84c464d` initially **failed**; user reran it manually and it succeeded. Root cause of the failure was never confirmed (no build log was shared), so it's unknown whether it'll recur on the next push — flagging as a real risk, not resolved just because a manual rerun worked once. Render service has no `render.yaml` in this repo, so build/start commands are configured directly in the Render dashboard, outside version control.
- **2026-07-10**: Added service account management — `POST /v1/service-accounts` (Principal + ServiceAccount + a Membership with a caller-specified role) and `POST /v1/service-accounts/:id/keys` (issues a key through the same `createApiKey` path a user's key uses). **Gap found and fixed as a necessary part of this slice, not scope creep**: `createApiKey` never set `ApiKey.principalId` at all before this — every workspace API key issued through the existing `/v1/workspaces/:workspaceId/api-keys` route was already failing every `requirePermission` check. Made `principalId` a required parameter of `createApiKey` (interface + both implementations); the existing route now resolves the target user's `User.principalId` first (400 if missing, rather than silently issuing an unusable key). `ensureSameWorkspace` moved from `workspaces.ts` into `rbac.ts` so the new route file doesn't have to reinvent the same cross-workspace check. New `service-accounts.test.ts` (9 tests) confirms a service-account-issued key gets identical allow/deny treatment to a human's key with the same role on a `workspace.manage`-gated route — required building a Prisma-backed test app variant (`createPrismaWorkspaceTestApp`) since `requirePermission` only ever reads real Postgres and the default test app is in-memory. Full `apps/api` suite: 134 tests passing. **Checklist**: "Service accounts as first-class principals" marked done for the API layer — list/revoke routes and dashboard UI are not built. **Next**: none of this session's service-account work has been run/verified against production yet.
- **2026-07-10**: Added an append-only `AuditEvent` table and wrote events inside the same Postgres transaction as the mutation they record, via a new `writeAuditEvent(tx, ...)` helper that only accepts a `Prisma.TransactionClient` — the type signature itself prevents calling it outside a transaction. **Scope, confirmed with the user before implementing**: covers the 7 routes already RBAC-gated from the last two slices; workspace creation and all of `onboarding.ts` stay unaudited, same reason as always — no real caller identity to attribute an event to. This is also the first use of `prisma.$transaction` anywhere in this codebase (confirmed via grep before starting) — worth knowing since it's a new pattern, not an established one. `PrismaWorkspaceService`'s constructor now accepts either `PrismaClient` or `Prisma.TransactionClient` so routes can scope it to the active transaction; `WorkspaceApiKeyRecord` gained an `id` field it was missing before (needed for the audit event's `targetId`). Added a new `audit.read` permission (Admin+Owner) — didn't exist in the taxonomy before this slice. **Verified, not just implemented**: the requested same-transaction test forces a genuine Postgres FK violation partway through a transaction (a `member.add` call with a `userId` that has no backing `User` row) rather than relying on a controlled early-return, and confirms the audit event count is unchanged afterward — this is a real rollback proof, not a control-flow coincidence. **Checklist**: "Audit log" marked done for the same 7-route surface as the RBAC/service-account work. **Next**: none of this has been run against production; the same `onboarding.ts` auth-story decision keeps recurring as the blocker for full coverage across RBAC, service accounts, and now audit logging — probably worth its own slice before extending any of these three further.
- **2026-07-10**: Ran `/db-migration-check production` — found the `AuditEvent` migration had not been deployed to Neon (`prisma migrate status` showed it pending). Applied it (`prisma migrate deploy`, purely additive) and re-ran `seed-rbac.ts` against production to add the new `audit.read` permission to Admin/Owner there too. Production schema now matches `main` exactly. **Still unconfirmed**: whether the actual Render deployment is running this code — data and schema are ready, application code readiness is still unverified (same gap as the earlier RBAC production catch-up).
- **2026-07-10**: Wrote a real integration test for the Phase 0 exit criteria (two orgs, two workspaces each, different roles, different router-model credentials, prove one workspace's admin can't see/spend another's budget) — `phase0-isolation.test.ts`, 4 tests, all passing against real Postgres. Budget isolation is proven with an actual `/v1/chat/completions` round trip, not a simulated assertion — worth noting it's backed by a structural guarantee (`workspaceId` comes from the authenticated API key, never a request param) rather than only an authorization check. **Scope decision, confirmed with the user before writing**: the task also asked to prove "cannot list workspace B's api keys" — there is no GET/list route for `ApiKey` anywhere in this codebase, for any workspace. Not a security gap; a missing feature. Declined to fabricate a test against a route that doesn't exist; reported the gap instead. `createPrismaWorkspaceTestApp` gained a mock provider, availability store, and a real `PrismaApiKeyAuthenticator` — it only wired up `requirePermission`'s auth path before, but `/v1/chat/completions` authenticates through the separate `authenticator.ts` path entirely. **Exit criteria verified, phase not complete**: the specific demo in the roadmap's Phase 0 exit-criteria sentence is now proven, but BYO Router Model and the declarative policy engine haven't been started, and RBAC/audit coverage is still scoped to workspace-management routes only (`onboarding.ts` remains open) — "Current phase" stays Phase 0.
- **2026-07-10**: Closed the gap flagged above — added `GET /v1/workspaces/:id/api-keys` (metadata only via an explicit Prisma `select`; `keyHash`/raw key never returned), gated on a new `apikey.read` permission (Admin+Owner). Updated `phase0-isolation.test.ts` to actually verify "cannot list workspace B's api keys" instead of documenting it as untestable — all 5 Phase 0 isolation tests now pass for real. Extended `workspaces-rbac.test.ts`'s role×route matrix and added a response-shape test. `apikey.read` seeded locally; **not yet re-run against production**. Full `apps/api` suite: 151 tests passing.
- **2026-07-10**: Started dashboard human-auth work — explicitly ahead of Phase 0's "don't do yet: dashboard polish" line, by direct user request, since the dashboard had zero session/login infrastructure at all (no cookies, no login screen, hardcoded workspace string, unauthenticated API calls). **Decision, made with the user before implementing**: rejected paste-an-API-key-into-the-browser as the login mechanism (API keys are long-lived high-privilege machine credentials — no recovery story, no invite flow, no SSO upgrade path, wrong trust model for humans). Built passwordless email magic-link login instead: `LoginToken` table (migration `20260710181857_add_login_token`), enumeration-safe `POST /v1/auth/request-link`, single-use `POST /v1/auth/verify` that exchanges a token for an HMAC-signed `routemind_session` cookie (hand-rolled HMAC-SHA256 + `timingSafeEqual`, consistent with this codebase's existing `security/api-key.ts` style — no JWT library). Email sending goes through a new pluggable `EmailSender` interface; only a `ConsoleEmailSender` (dev-mode, logs to stdout) is wired up — **real provider (Resend/SES) wiring is deferred until the user has credentials**, per explicit choice. Dashboard got `/auth/login` (email entry → check-your-email state) and `/auth/callback` (exchanges the token, then redirects to the existing `/dashboard` route — **there is no workspace switcher yet**, that's future work, so this redirect target is a known placeholder, not an oversight). New required `SESSION_SECRET` env var (dev default provided, `validateProductionConfig` fails the deploy if left at the default — **not yet set in Render's production environment**) and `DASHBOARD_URL`. **Also fixed**: `apps/dashboard/tsconfig.json` had inherited `lib: ["ES2022"]` with no DOM types from `tsconfig.base.json` — nothing had broken before because this is the first `onChange` handler ever written in this app; added `"DOM", "DOM.Iterable"` to its `lib` array. Verified: 157/157 `apps/api` tests, lint clean, `apps/dashboard` build clean, and a live smoke test against a running dev server (request-link → console-logged email → verify → session cookie set → reuse correctly rejected with 401). **Not done, explicitly out of scope for this slice**: `LoginToken` migration not yet applied to production; API client doesn't send `workspaceId` yet; no workspace switcher UI; individual feature screens untouched.
- **2026-07-11**: Built the membership invite flow on top of the magic-link infra. Added `WorkspaceInvite` (migration `20260711041002_add_workspace_invite`, purely additive) and `POST/GET /v1/workspaces/:id/invites` + `DELETE .../invites/:id`, all gated on `workspace.manage` — reuses the `LoginToken` hash/email pattern for delivery rather than a parallel mechanism. `POST /v1/auth/verify` now tries a `LoginToken` first, then falls back to a `WorkspaceInvite` lookup by the same token hash: on first acceptance it creates the User/Principal if the invited email has no account yet, upserts both the RBAC `Membership` and the legacy `WorkspaceMember` row (via `PrismaWorkspaceService.addMember` inside the same transaction, so the two coexisting member systems stay in sync for this path), marks the invite accepted, and returns `{ user, workspace }` so the dashboard callback can land the invitee directly in that workspace instead of a generic page. **Blocker found and fixed as necessary, not scope creep**: the dashboard's session cookie had _no_ way to call any RBAC-gated route — `requirePermission` only ever accepted a machine API key. Without fixing this the new Members screen (list/invite/role-change/revoke) would be non-functional, so `requirePermission` now falls back to the `routemind_session` cookie when no API key is present, resolving `Membership` from the route's `:workspaceId` param (a session isn't pinned to one workspace, unlike an API key). All 9 existing call sites (`workspaces.ts`, `service-accounts.ts`, `rbac.test.ts`) updated to pass `config`; behavior for machine API-key callers is unchanged. Also enriched `GET /v1/workspaces/:id/members` with each member's email/name (a bulk `prisma.user.findMany` after the existing `listMembers` call) since the dashboard has nothing else to display. New dashboard `/dashboard/members` screen: invite-by-email form, active members table (role dropdown + remove), pending invites table (revoke) — calls the existing member PATCH/DELETE routes plus the new invite routes, all via `credentials: "include"`. `getActiveWorkspace()`/`setActiveWorkspace()` (localStorage) is the minimal workspace-context plumbing this needed; the real switcher (multi-membership picker) is still separate, deferred work. **Verified**: 165/165 `apps/api` tests passing (157 existing + 8 new in `invites.test.ts`, including a same-transaction accept-flow test and a session-cookie-only RBAC call), lint clean, both `apps/api` and `apps/dashboard` builds clean. **Not done, explicitly out of scope for this slice**: migration not yet applied to production; workspace switcher UI; per-request `workspaceId` propagation for the other dashboard pages; consolidating the legacy `WorkspaceMember`/RBAC `Membership` dual system (both are still written to, as before).
- **2026-07-11**: User set `SESSION_SECRET` in Render's production environment (closing the gap flagged on 2026-07-10) and redeployed — user-confirmed successful. This session had no Render API/CLI access to do it directly or to verify the deploy itself, so verification was via the user's report only. **Still unconfirmed**: whether that deploy's build/start step actually ran `prisma migrate deploy` — there's no `render.yaml` in this repo, so it's unknown whether migrations apply automatically or need a manual step, meaning `20260711041002_add_workspace_invite` (and the invite flow it backs) may still not be live against the production DB. Worth an explicit `/db-migration-check production` pass before relying on the invite flow in production.
- **2026-07-11**: Ran `/db-migration-check production` — confirmed all 18 migrations, including `20260711041002_add_workspace_invite`, are applied against the production DB (Render's deploy pipeline does run migrations automatically, resolving the open question from the previous entry). While checking, found `Admin`/`Owner` in production were still missing `apikey.read` (Admin 7/8, Owner 9/10 permissions) — the gap explicitly flagged as unresolved in the 2026-07-10 entry ("seeded locally; not yet re-run against production") had never been closed. Re-ran `seed:rbac` against production (idempotent); both roles now match local dev exactly. Owner already had `workspace.manage`, so the invite flow itself was never blocked by this — only `GET /v1/workspaces/:id/api-keys` was silently 403ing for everyone in production until now. **Flagging, not fixing here**: the user pasted the live production Postgres (Neon) connection string, including its password, directly into chat during this session — same pattern as a prior session's Neon/Upstash exposure. That password should be rotated, independent of this work being otherwise complete.
- **2026-07-11**: Added RBAC-driven UI gating to the dashboard. New `GET /v1/workspaces/:id/me` returns the caller's role + effective permissions for that workspace (`rbac.ts`'s identity/membership resolution split into `resolveIdentity`/`resolveMembership`, shared with `requirePermission` — same 401/403 semantics, verified unchanged by the existing `rbac.test.ts` suite). Dashboard gets a `PermissionsProvider` (new `app/dashboard/layout.tsx`, wraps every `/dashboard/*` page) that fetches it once per active workspace. Members page's mutating actions (invite, role change, remove, revoke) are now `disabled` with a `title="Requires <Role>"` tooltip instead of just failing with a 403 after the click. Added "Audit Log" as a new nav item + minimal read-only page — the backend route (`audit.read`-gated) already existed with no dashboard UI at all — shown greyed out with a lock icon and tooltip rather than removed when the caller lacks the permission, matching the task's explicit example. **Scope decision, stated up front**: API key management, provider-credential editing, and budget management were explicitly NOT touched — none of those have any dashboard UI yet (confirmed via exploration), so there was nothing to gate; building those screens is separate, larger work for a future slice. **Verified**: 169/169 `apps/api` tests (165 existing + 4 new in `me.test.ts`), including the specific ask — a real Owner invites a real Developer through the actual invite-accept flow from the last slice, and `/me` is asserted to return two different, correctly-scoped permission sets for the same workspace, not two independently-seeded fixtures. Lint clean, both `apps/api` and `apps/dashboard` builds clean. **Not done**: no browser present in this environment to visually confirm the greyed-out nav item/tooltips actually render as intended — the gating logic type-checks and follows the same `disabled`/`title` pattern already used elsewhere in this codebase, but that's not the same as having looked at it.
- **2026-07-11**: Built the audit log dashboard screen. `GET /v1/workspaces/:id/audit-log` (already `audit.read`-gated from the previous slice's inline write coverage) now supports cursor-based pagination (`limit`/`cursor`, stable `[createdAt desc, id desc]` ordering) and `principalId`/`action`/`from`/`to` query filters — no schema change, `AuditEvent` already had every column needed. Each event is enriched with the acting `Principal.displayName` so the table shows a name instead of a raw cuid. Dashboard's Audit Log page (built last slice as a bare read-only list) gained filter controls and a "Load more" button; the existing greyed-out-nav/`EmptyState` RBAC treatment from the last slice is unchanged. **The specific ask, verified**: a new test performs a real invite → accept → role-change → API-key-issuance sequence (not synthetic fixtures) and confirms each resulting event attributes to the correct principal — the accepting Developer's own `member.add` event is attributed to _them_, while the invite/role-change/apikey-create events the Owner performed on their behalf are attributed to the _Owner_, and the two principalIds are asserted distinct. **Gotcha worth remembering**: the three new tests initially failed because `buildApp({ config, prisma })` without an explicit `workspaceService` silently defaults to `InMemoryWorkspaceService`, which doesn't see rows written by the real `PrismaWorkspaceService` the invite-accept flow uses internally — any new test that mixes `/v1/auth/verify`-driven membership creation with other workspace routes must pass `workspaceService: new PrismaWorkspaceService(prisma)` explicitly, same as `createPrismaWorkspaceTestApp` already does. **Verified**: 172/172 `apps/api` tests (169 existing + 3 new), lint clean, both apps build clean. **Checklist**: "Audit log" stays at its existing partial-done annotation — this slice improved the _read_ side (pagination/filtering/UI) but did not expand _write_ coverage; `onboarding.ts` and workspace creation remain unaudited, unchanged from before.
- **2026-07-12**: Built the service accounts dashboard screen — create, key issuance, key revocation, all gated the same way as Members/Audit Log. **Real gap found and fixed as necessary, not scope creep**: the two existing service-account routes took `workspaceId` in the request body rather than the URL, so `requirePermission`'s session-cookie path (which can only resolve identity from a `:workspaceId` route param, added two slices ago) could never authenticate a dashboard caller against them — they were only ever callable with a machine API key. Restructured both to `POST/GET /v1/workspaces/:workspaceId/service-accounts` and `POST .../service-accounts/:id/keys`, updating the existing 9-test `service-accounts.test.ts` to match. Added the list route (`apikey.read`-gated — Admin+Owner both need to see accounts they're allowed to act on, matching the task's own create/issue/revoke→workspace.manage/apikey.create/apikey.delete permission mapping) with keys embedded per account, and a revoke-key route (`apikey.delete`-gated — a permission that existed in `seed-rbac.ts`'s taxonomy since Phase 0 but had never been used by any route until now; sets `ApiKey.isActive = false`, new `apikey.delete` audit action). **Gotcha, same class as the last two slices**: three of the new tests initially failed because they used the in-memory-workspace-service test fixture (`setupForRole`) while the new list/revoke routes read `ApiKey` rows directly from Postgres — fixed by switching those specific tests to `createPrismaWorkspaceTestApp`. **The specific ask, verified**: a dedicated test creates a service account, issues a key, revokes it, then reads all three events back through last slice's `GET /v1/workspaces/:id/audit-log` (including its `action=` filter) and confirms each is present, correctly attributed to the acting Owner (not the service account), and correctly targeted. **Verified**: 178/178 `apps/api` tests (172 existing + 6 net new), lint clean, both apps build clean. **Checklist**: "Service accounts as first-class principals" now marked fully done — the "list/revoke routes and dashboard UI not built" gap from two slices ago is closed. **Not done**: deleting the service account entity itself (only its keys can be revoked — matches what was actually asked); no browser in this environment to visually confirm rendering, same standing caveat as the last two slices.
- **2026-07-12**: Wrote and ran a manual QA script (`apps/api/src/scripts/qa-phase0-dashboard-flow.ts`, `npm run qa:phase0 -w apps/api`) walking the full Phase 0 dashboard flow end-to-end against real local Postgres via `app.inject()` — the most faithful thing achievable without a browser in this environment. Reports PASS/GAP/FAIL per step instead of asserting, and cleans up everything it creates. **Explicitly not fixed, per instruction — reported instead**: (1) **GAP** — no self-serve sign-up exists anywhere; `POST /v1/auth/request-link` is enumeration-safe by design but genuinely creates nothing for an unknown email, so a User row must already exist (via invite or manual provisioning) before magic-link login works at all — matches Phase 1's not-yet-built "self-serve signup" item, confirmed still accurate. (2) **GAP** — an ordinary (non-invite) login sets no active workspace: `POST /v1/auth/verify`'s `workspace` field is only populated on first-time invite acceptance, so a _returning_ user normally logging in lands on `/dashboard` with nothing in localStorage and every workspace-scoped page (Members/Audit Log/Service Accounts) immediately shows its empty state — there's no dashboard affordance to pick a workspace otherwise. (3) **FAIL, more severe than expected** — `POST /v1/workspaces` has no permission gate at all (confirmed: succeeded using a valid session that was never actually checked) and defaults `ownerUserId` to the literal string `"dev-user"` when not supplied, which the dashboard never supplies; running it for real produced an unhandled 500 (`WorkspaceMember_userId_fkey` violation) rather than a clean error, and left an orphaned `Workspace` row in the dev DB (manually cleaned up; the script's cleanup logic was then fixed to catch this case on future runs) — even a call that _did_ supply a real `ownerUserId` would still never create a `Membership`/`Principal`, permanently locking that "owner" out of every RBAC-gated route in the workspace they supposedly own. Five other steps (invite via session cookie, invite-acceptance landing, Developer-role gating via both `/me` and direct 403 checks on 4 gated actions, service-account create/issue/revoke, and audit-log attribution/filtering) all **PASS** — confirms the last three slices' work is solid end-to-end. **Next**: workspace creation (gate it, stop trusting a client-supplied `ownerUserId`, actually create the `Membership`) is the one finding here worth prioritizing before Phase 1 — a stranger being able to unauthenticated-500 the API, or a real caller ending up permanently locked out of a workspace they just created, are both real production risks, not cosmetic gaps. **QA script run: 5 pass, 2 gap, 1 fail — see known issues above.**
- **2026-07-12**: Fixed two of the three QA-flagged known issues (commit `ebcd471`). **Self-serve signup**: `LoginToken.userId` is now nullable with an added `email` column (migration `20260712170417_login_token_self_signup`), so `POST /v1/auth/request-link` issues a token for unknown emails too (storing `email` directly, same pattern as `WorkspaceInvite`) instead of silently doing nothing. `POST /v1/auth/verify` now branches on `loginToken.userId` being null: creates User + Principal + Organization + Workspace + Owner Membership + legacy `WorkspaceMember` + an audit event (`member.add`, `metadata.viaSignup: true`) in one `prisma.$transaction`, so any failure partway through leaves zero rows. **Verified, not just implemented**: the workspace `slug` is deliberately derived from `loginToken.id` (not `principal.id`) specifically so a test could predict it and pre-occupy that exact slug, forcing a genuine Postgres unique-constraint violation _after_ Principal/User/Organization were already written in the same transaction — proving a real rollback, not a controlled early-return (mirrors the same-transaction proof technique already used in `audit-log.test.ts`). **Workspace creation**: `POST /v1/workspaces` now requires `organizationId` in the body (the client-supplied `ownerUserId`/`"dev-user"` fallback is gone entirely) and is gated by a new `requireOrganizationPermission` helper in `rbac.ts` — since no org-level Membership/Role table exists, it grants access if the caller has `workspace.manage` via a Membership in _any_ workspace belonging to the target org (session cookie or API key, same identity resolution as `requirePermission`, just not pinned to a `:workspaceId` route param since the workspace doesn't exist yet). The creator becomes the new workspace's Owner (RBAC Membership + legacy `WorkspaceMember`, mirroring the pattern from earlier RBAC slices). **Decision**: the pre-existing `workspaces.test.ts` "creates a workspace" test called the route unauthenticated with the old `ownerUserId` field — rewritten (not preserved) since that exact behavior is the bug being fixed; a new dedicated 401 test replaces it, per this slice's explicit ask. **Also delivered**: a read-only `find-orphaned-workspaces.ts` script (`npm run check:orphaned-workspaces -w apps/api`) that flags any `Workspace` with zero `Membership` and zero legacy `WorkspaceMember` rows — run against local dev Postgres, found 0 (the one orphan from the 2026-07-12 QA run had already been cleaned up manually per the entry above). **Gotcha**: `prisma generate` failed repeatedly with `EPERM` on the query-engine DLL because a stray `npm run dev` process (PID 21684) still held it open from an earlier session — killed with the user's explicit confirmation before regenerating successfully. **Verified**: `apps/api` build clean, lint clean, and the full targeted suite (`auth.test.ts` 8/8, `workspaces.test.ts` 6/6, plus `workspaces-rbac.test.ts`/`invites.test.ts`/`audit-log.test.ts`/`service-accounts.test.ts` unaffected at 84/84) all passing; the full monorepo `apps/api` suite was not re-run after this (only the targeted + adjacent RBAC files), per the slice's own "not the whole monorepo unless cross-cutting" guidance. **Next**: the third known issue (normal login doesn't land the user in a default workspace) is the next slice, already queued.
- **2026-07-12**: Re-ran `npm run qa:phase0 -w apps/api` against current code (including today's two fixes) per explicit instruction to report only, not fix anything found. **Result: 5 PASS, 1 GAP, 2 FAIL** (previous run: 5 pass, 2 gap, 1 fail). **Both FAILs are the QA script itself being stale, not regressions** — verified against the dedicated tests from the prior slice, which independently confirm both fixes work: (1) Step 1 (signup) only calls `POST /v1/auth/request-link` and checks its old assumption that an unknown email sends 0 emails; today's fix intentionally sends one now, so the script's hardcoded check flags "unexpected" — it never calls `/v1/auth/verify` to actually exercise the new signup transaction, so it doesn't test the fix positively or negatively either way. (2) Step 3 (create a second workspace) still posts `{ name: "QA Second Workspace" }` with no `organizationId`, which is now a required field — gets a clean `400` instead of the old `500` crash (real progress, no more orphaned row), but still counts as FAIL since the script wasn't updated to the new request contract. Step 2 (default workspace on login) is unchanged, correctly still GAP — that's the next slice, not yet done. **New finding from this re-run**: the QA script's cleanup only deletes `LoginToken` rows by `userId`, so the throwaway signup email from Step 1 leaves an orphaned `userId: null` token behind (harmless — expires in 15 minutes, never usable, no FK dependents). Same gap exists in `auth.test.ts`'s own `afterEach` cleanup — found 3 more matching orphans from this session's test runs. Confirmed via direct query: `Workspace`/`Organization` counts stayed at 3/3 (unchanged), so the failed Step 3 attempt correctly created nothing. **Nothing was fixed** — script, test cleanup, and the 4 orphaned `LoginToken` rows are all left as observed, per the explicit "just report" instruction. **Next**: the QA script's Steps 1 and 3 need updating to the new contracts (send `organizationId`, call `/verify` to complete the signup check) before it can be trusted as a regression check again; the `LoginToken` cleanup gap (script + `auth.test.ts`) is a small standalone fix worth bundling into whichever slice touches either file next.
- **2026-07-12**: Updated `qa-phase0-dashboard-flow.ts` Steps 1 and 3, confirming both prior FAILs were the script using a stale request/response shape, not real bugs (commit `e6dc15f`). Step 1 now runs the full signup flow — extracts the token from the sent email, calls `/v1/auth/verify`, asserts the User/Workspace/session cookie were actually created, and registers the new rows for cleanup (it previously stopped right after `request-link` and asserted on an inverted, now-wrong condition). Step 3 now sends the required `organizationId`, asserts the caller becomes the new workspace's Owner via a real `Membership` row, and adds a second unauthenticated call to the same route to prove the permission gate rejects it (401) — replacing the old GAP/FAIL narrative text that described the pre-fix bug (no gate, `dev-user` fallback, missing `Membership`) verbatim regardless of what actually happened. Also corrected a `Bootstrap`-block comment that still claimed "there's no self-serve signup," directly contradicted by Step 1 three lines above it. **Re-run result: 7 PASS, 1 GAP, 0 FAIL** (previously 5 PASS, 1 GAP, 2 FAIL) — confirmed twice, including after the comment fix. **Verified**: `apps/api` build and lint clean; cleanup confirmed complete via `check:orphaned-workspaces` (still 3 workspaces, 0 orphans, unchanged). **Not done, still open**: the `LoginToken` cleanup gap (script + `auth.test.ts` both only delete tokens by `userId`, missing the new email-only orphan path) was flagged again but intentionally not touched — out of scope for this slice, which was limited to Steps 1 and 3. **Next**: the one remaining known issue (default workspace on normal login, Step 2's GAP) is the only thing left before Phase 0's known issues are fully closed.
- **2026-07-18**: Re-ran `npm run qa:phase0 -w apps/api` against current code per explicit instruction to report only, not fix anything. **Result: 7 PASS, 1 GAP, 0 FAIL — unchanged from the 2026-07-12 run.** Local dev Postgres/Redis containers (`llm-router-postgres-1`/`llm-router-redis-1`) had stopped since the last session and were restarted to run the script; no app code or schema touched. Steps 1, 3, 4, 5, 6, 7-9, and 10 all still PASS exactly as last recorded. Step 2 is still the sole GAP: `POST /v1/auth/verify`'s response has no `workspace` field for an ordinary (non-invite) login, so a returning user lands on `/dashboard` with no active workspace in localStorage and every workspace-scoped page shows its empty state. Cleanup re-verified via `check:orphaned-workspaces` (3 workspaces, 0 orphans, unchanged). **Nothing fixed, nothing committed** — this was a pure regression check. **Next**: Step 2's fix (land a returning user in a default/most-recent workspace on normal login) remains the last open item before Phase 0's known issues are fully closed.
- **2026-07-18**: Fixed Step 2's GAP — the ordinary (non-invite) login branch of `POST /v1/auth/verify` (`apps/api/src/routes/auth.ts`) now looks up the user's `Membership` by `principalId`, orders by `createdAt desc`, and returns that workspace in the response (`{ user, workspace }`), matching the shape the signup and invite-acceptance branches already used. **Decision**: `Membership` has no `lastActiveAt`/`updatedAt` column, so "most recent" is approximated as most-recently-created membership rather than adding a migration for this slice — flagged as the honest limitation, not silently glossed over; a real "last active workspace" would need a schema change in a future slice if this approximation proves wrong in practice (e.g. a user's _oldest_ membership is the one they actually use daily). **Zero-membership case**: returns a 400 with a clear message instead of a silently-undefined `workspace` field, per explicit instruction — shouldn't happen post-signup-fix (every account gets an Owner Membership on creation) but handled rather than assumed impossible. **Verified**: two of the three pre-existing tests that log in via this branch (`verifies a valid token...`, `rejects a token that has already been used`) needed their test users upgraded from principal-only to principal+workspace+membership (a new `createUserWithWorkspace` test helper), since the branch now hard-requires a membership to succeed — this is an intentional behavior change, not test breakage to paper over. Added two new dedicated tests: one asserting the correct workspace comes back for a single-membership login, one asserting the 400/no-token-consumed behavior for a membership-less account. `apps/api/src/routes/auth.test.ts`: 12/12 passing (10 previously + 2 new). Full RBAC-adjacent suite (`auth`, `invites`, `workspaces`, `workspaces-rbac`, `me`, `audit-log`, `service-accounts`, `phase0-isolation`): 88/88 passing. Lint and `apps/api` build both clean. **Not done, out of scope for this slice**: the dashboard frontend still doesn't call this new `workspace` field to actually set the active workspace on a normal-login redirect — that's a follow-up dashboard-side slice, not covered here. **Checklist**: all three 2026-07-12 QA-flagged known issues are now closed.
- **2026-07-18**: Investigated the dashboard-side follow-up flagged above and found the claim was wrong — `apps/dashboard/src/app/auth/callback/page.tsx`'s existing logic checks `if (result.workspace)` generically, not gated on an invite-specific flag, so it already called `setActiveWorkspace()` and redirected into the workspace for an ordinary login as soon as the backend slice above started returning `workspace` on that branch. There was no frontend runtime bug left to fix. The real defect was that the surrounding comments (in `callback/page.tsx`, `auth-client.ts`'s `VerifyLoginTokenResult.workspace` JSDoc, and `workspace-client.ts`'s module comment) all still described this as invite-only, which was actively misleading about behavior that now covers signup and ordinary login too — fixed all three to accurately describe the tri-branch behavior. **Manual QA, since the backend-only `qa:phase0` script can't observe frontend wiring**: ran a live round trip against the real dev API (signup verify, then a second request-link + verify for the same now-existing email — the exact ordinary-login branch) and confirmed the JSON response is `{"user":{...},"workspace":{"id":"...","name":"Default"}}`, i.e. the exact shape `postJson()` hands to the callback page. Combined with the code read confirming `setActiveWorkspace`/`router.replace("/dashboard/members")` already fire on any truthy `workspace`, this is sufficient evidence the wiring works end-to-end for the JSON/localStorage layer. **Not verified, same standing caveat as every prior dashboard slice**: no real browser in this environment to visually confirm the actual redirect and populated Members page render correctly. **Verified**: `apps/dashboard` production build (`next build`) compiles and type-checks clean; `apps/dashboard` currently has no automated test suite at all (confirmed via search) and is excluded from the root ESLint file matching in this repo's current (already-modified, pre-existing) `eslint.config.mjs` — both gaps pre-date this slice and were left untouched. **Decision, flagged not silently made**: intentionally did not change the redirect destination (`/dashboard/members` for every branch that returns a workspace, including brand-new signups) — distinguishing "just signed up," "just logged in," and "just accepted an invite" to pick a more tailored landing page would need a backend discriminator field, which is a real product decision beyond this slice's scope, not a bug fix. **Next**: none — this closes out the frontend half of Step 2's fix; a future slice could add that discriminator if landing every new signup on the Members page (rather than an overview) turns out to be the wrong default.
- **2026-07-18**: Two verification passes, no code changes. **(1) Re-ran `npm run qa:phase0 -w apps/api`: 8 PASS, 0 GAP, 0 FAIL** — Step 2 (previously the sole GAP) now reports PASS on its own, since the script checks the real response for a `workspace` field rather than asserting a hardcoded outcome; no script edit was needed, the last two slices' fixes were sufficient. **(2) Closed the "no real browser in this environment" caveat that's recurred in every dashboard-touching session log entry since 2026-07-11** — booted the actual `apps/api` (port 3000) and `apps/dashboard` (port 3002) dev servers against the same local Docker Postgres/Redis, and drove a real Chromium browser via Playwright MCP tools through the full flow: signed up a fresh email (`manual-qa-browser-test@routemind-qa.local`) via the real `/auth/login` form, confirmed it landed on `/dashboard/members` showing the new workspace ("Default") with the signed-up user listed as owner; then cleared `localStorage` to simulate a returning user with no cached active workspace, requested a second login link for the _same_ now-existing email (confirmed via the API's console log this used "Click to log in to" phrasing, not "create your account" — the ordinary-login branch, not signup), and verified the callback again landed on `/dashboard/members` with the same real member data rendered — not the "no active workspace" empty state. This is the first slice in Phase 0's history with an actual pixel-rendered confirmation rather than a code-read-plus-JSON-contract argument. **Cleanup**: deleted the test user/workspace/organization/membership rows via an ad hoc script (not committed), re-verified via `check:orphaned-workspaces` (3 workspaces, 0 orphans, unchanged), and removed the Playwright session artifacts (`.playwright-mcp/`, a screenshot) that landed in the repo root — none of that is committed. **Nothing else changed**: no source files touched this slice, so no code commit; this entry is the only change. **Checklist**: Phase 0's known-issues list (schema migration, RBAC, service accounts, audit log, BYO Router Model, declarative policy engine) is unaffected — BYO Router Model and the declarative policy engine remain the two still-unstarted Ship items keeping Phase 0 open.
- **2026-07-18**: Started BYO Router Model — first slice is schema + resolver only, explicitly not wired into anything yet. Added `RouterConfig` (migration `20260718165438_add_router_config`): `scopeType` (`org` \| `workspace` \| `api_key`) + `scopeId`, `provider`, `model`, `fallbackModel`, `credentialId` (real FK to `ProviderCredential`, `onDelete: SetNull`). **Decision, flagged via `db-migration-check` rather than silently applied**: `scopeId` is a polymorphic reference with no enforced FK — Prisma can't express "references one of Organization/Workspace/ApiKey depending on scopeType," the same tradeoff `WorkspaceInvite` already accepts for its email-only path. This is a deliberate exception to `.claude/rules/prisma.md`'s workspace-scoping rule (`RouterConfig` has neither `workspaceId` nor `userId`), matching what was explicitly asked and what `docs/architecture.md`'s own proposed schema shows — flagged as an open question rather than assumed settled: a three-nullable-FK alternative (`organizationId?`/`workspaceId?`/`apiKeyId?` with exactly one set) would get real referential integrity at the cost of three mutually-exclusive columns, worth a second look before this becomes load-bearing. `packages/shared/src/types` was **not** updated, matching the existing precedent (`Organization`/`Membership`/`Principal`/`ServiceAccount`/`AuditEvent` never got shared types either) — no consumer exists yet to derive them from. Added `resolveRouterConfig()` in `packages/routing/src/router-config.ts` implementing the fallback chain (request override → api_key → workspace → org → platform default) as a pure function taking an injected `RouterConfigLookup` interface rather than a `PrismaClient`, keeping `packages/routing` framework-free per `.claude/rules/routing-engine.md` and unit-testable with an in-memory fake — a real Prisma-backed `RouterConfigLookup` adapter would live in `apps/api/src/infrastructure/` in a future slice, not built here. 8 new unit tests in `router-config.test.ts` cover every fallback level (request/api_key/workspace/org/platform_default), the "scope id absent from context is skipped, not queried as a miss" case, and that optional `fallbackModel`/`credentialId` survive resolution. **Verified**: `packages/routing` build + lint clean, 8/8 new tests passing, full `apps/api` suite (183 tests) still green after the migration, `apps/api` build clean. **Gotcha, same as a prior session**: `prisma generate` hit the same `EPERM` on the query-engine DLL seen before — this time a bare retry succeeded without needing to kill any process. **Not done, explicitly out of scope for this slice**: not wired into `decideLLMRoute`/`routeChatCompletion`, no CRUD routes for managing `RouterConfig` rows, no dashboard UI, no real Prisma-backed lookup adapter, migration not yet applied to production. **Checklist**: BYO Router Model moves from "not started" to "schema + resolver done" — still the furthest-behind of the two remaining Ship items keeping Phase 0 open, alongside the declarative policy engine.
- **2026-07-18**: Ran `/db-migration-check prod` on the `RouterConfig` migration, then deployed it to production at explicit request. `prisma migrate status` against Neon showed 19/20 migrations applied, only `20260718165438_add_router_config` pending (expected — it wasn't pushed to `origin/main` yet at that point). Read `migration.sql` first to confirm it was purely additive (one new `CREATE TABLE`, no ALTERs to existing tables) before running `prisma migrate deploy`; re-ran `migrate status` afterward to confirm "Database schema is up to date!" across all 20 migrations. Pushed the pending commits (`6cae5e0`, `1062d88`) immediately after so the repo and prod schema wouldn't drift apart. **Flagging, not fixing here**: the user pasted the live production Neon connection string (including password) directly into chat to enable this check — the third occurrence of this exact pattern this project (2026-07-10, 2026-07-11, now 2026-07-18), despite being offered two credential-free alternatives first. Rotating that password is still outstanding after three flags.
- **2026-07-18**: Re-invoked with the exact same BYO Router Model schema+resolver prompt as the slice above — turned out to be a stale/duplicate `/dev-loop` trigger, since that work was already committed, pushed, and deployed. Confirmed via `git log`/`grep` before doing anything, then asked which real next step to take; user chose wiring `resolveRouterConfig()` into the actual routing engine. **Investigation note, worth remembering**: the first exploration subagent for this new slice returned a report with two confidently-stated but false claims (`RouterConfig` schema "missing," `apps/api/src/security/credentials.ts` "does not exist yet") — both contradicted by direct reads in the same session (the file exists; `decryptCredential` is actively used in `user-availability.ts`). Root cause: `apps/api/src/security/` is permission-restricted for this session (Read/Grep/Bash all denied on `credentials.ts`), and the subagent apparently fabricated a plausible-sounding gap instead of surfacing that it was blocked. **Lesson applied**: no longer trusting delegated exploration reports at face value when they touch permission-restricted paths — spot-checked the next subagent's claims with a direct read (`app.ts`'s `createDefaultAuthenticator`) before proceeding, which matched exactly. Concluded "wire into the routing engine" is genuinely 3-4 separate slices (identity-context plumbing, a Prisma-backed `RouterConfigLookup` adapter, the actual `LiveRouterLLMService`/`chat-completions.ts` wiring plus response-metadata changes, and a separate decision on a per-request override schema field) — stopped and asked which to build first per the dev-loop skill's explicit "propose splitting" instruction, rather than attempting all of it as one commit. User picked the identity-plumbing slice; see the next entry for what shipped.
- **2026-07-18**: Shipped the identity-plumbing sub-slice: extended `AuthenticatedUser` (`apps/api/src/infrastructure/authenticator.ts`) with optional `apiKeyId` and `organizationId` fields, needed because `resolveRouterConfig()`'s fallback chain requires the caller's API key id and org id, neither of which existed on this type before (only `userId`/`workspaceId` did). Updated `PrismaApiKeyAuthenticator`'s raw SQL query to also select `ApiKey.id` and LEFT JOIN `Workspace` for `organizationId` (nullable, since `Workspace.organizationId` itself is nullable), and updated `app.ts`'s in-memory test-path authenticator to populate `apiKeyId` from its own record (no `organizationId` equivalent exists there, since `InMemoryWorkspaceService` doesn't model organizations at all — left `undefined`, not faked). **Explicitly no behavior change**: both fields are additive and optional; nothing reads them yet. Added 2 real-Postgres tests to `authenticator.test.ts` (using the existing `seedWorkspaceWithRole`/`cleanupFixture` fixtures) proving `apiKeyId` and `organizationId` populate correctly for a workspace-scoped key, and that `organizationId` stays `undefined` (not null, not a crash) for a workspace with no organization. **Verified**: 3/3 authenticator tests passing, full `apps/api` suite (185 tests, 183 previous + 2 new) still green, lint and build both clean. **Checklist**: BYO Router Model now has schema + resolver + identity plumbing done. **Next**: the remaining sub-slices — a real `PrismaRouterConfigLookup` adapter, then actually wiring `resolveRouterConfig()` into `LiveRouterLLMService`/the `chat-completions.ts` call site (decrypting a resolved `credentialId` via the existing `decryptCredential`, surfacing the resolution source in response metadata) — are still unstarted; the per-request override schema field is a separate open decision, not yet made.
- **2026-07-19**: Shipped the actual wiring: `LiveRouterLLMService` (`apps/api/src/infrastructure/router-llm-service.ts`) no longer hardcodes `ROUTER_LLM_MODEL`/`"openai"` — it now calls `resolveRouterConfig()` via a new `PrismaRouterConfigLookup` (`apps/api/src/infrastructure/router-config-lookup.ts`, a thin `RouterConfig.findUnique({scopeType_scopeId})` wrapper) and uses whichever provider/model that resolves to. **Design correction made mid-slice**: the first draft had `LiveRouterLLMService` call `createProviderRegistry()` directly to build an adapter for a resolved `credentialId` — reconsidered after realizing this makes the credential path untestable (its "mock" mode returns hardcoded classes whose `chatCompletion` returns plain text, not the JSON a router decision needs, and "live" mode would require real network-capable adapters in a test). Made the provider factory an injected constructor dependency (`RouterCredentialProviderFactory`) instead, matching this codebase's existing DI pattern (`chat-completions.ts` already takes a `providerFactory`) — `app.ts` passes the real `createProviderRegistry`-backed one in production, tests inject a controllable fake. `chat-completions.ts`'s `routerLLMServiceFactory` now threads `{apiKeyId, workspaceId, organizationId}` context through (using the fields added last slice). **Debugging note worth remembering**: the new end-to-end test initially failed with zero calls to either fake adapter and no visible error — traced to `workspaces-rbac-fixtures.ts`'s shared `testConfig` having `ROUTER_LLM_ENABLED: false` (distinct from `chat-completions.test.ts`'s own local `testConfig`, which has it `true`), silently skipping the entire llm_assisted branch in `decideLLMRoute` before ever reaching the router service — `decideLLMRoute`'s catch-all "Router LLM skipped or invalid" fallback reason string doesn't distinguish "disabled" from "threw an exception" from "decision didn't match a candidate," which cost real debugging time (a temporary `LiveRouterLLMService.decide()` console.log and a standalone direct-call script were needed to isolate it) — worth overriding `ROUTER_LLM_ENABLED: true` explicitly in any future test that expects the router-LLM path to run. New test (`router-config-wiring.test.ts`, 2 cases) seeds a real workspace + `ProviderCredential` + `RouterConfig` row via Prisma, sends a real `/v1/chat/completions` request, and asserts: the credentialed fake "anthropic" adapter (deliberately absent from the ambient providers map) was called with the resolved model, the injected provider-factory spy received the correctly _decrypted_ key, and a control workspace with no `RouterConfig` row instead calls the ambient adapter with the platform-default model. **Verified**: full `apps/api` suite 187/187 passing, lint and build clean. **Not done, explicitly deferred**: response metadata still doesn't surface which scope a config resolved from; no CRUD routes or dashboard UI for managing `RouterConfig` rows; no per-request override field. **Checklist**: BYO Router Model's live wiring is done — remaining work is CRUD/UI/observability, not core mechanism.
- **2026-07-19**: Fixed the exact silent-fallback bug flagged by the previous slice's own debugging note. `decideLLMRoute` (`packages/routing/src/index.ts`) previously computed one generic `"Router LLM skipped or invalid; used score_based fallback"` string at the final return, regardless of which of several genuinely different causes produced it. Now assigns a distinct, exported reason constant (`LLM_ROUTING_DISABLED_REASON`, `LLM_ROUTING_STRATEGY_SKIP_REASON`, `LLM_ROUTING_CALL_FAILED_REASON`, `LLM_ROUTING_NO_CANDIDATE_MATCH_REASON`) at the actual point each case diverges, reusing the existing string-based `routingReason`/`routingMetadata.routerReason` fields per `.claude/rules/routing-engine.md`'s existing contract rather than adding a new coded field. **Found and fixed a fourth case beyond the three named in the ask**: the `cost_first && simple_chat && low-complexity` strategy guard was silently collapsing into the same generic string too — same bug class, fixed alongside the other three rather than left half-done. Added `packages/routing/src/decide-llm-route.test.ts` — the first-ever direct unit test for `decideLLMRoute` (previously only exercised indirectly through `apps/api`'s HTTP-level tests) — covering all four fallback reasons, the unaffected success path, and an explicit "all four reasons are mutually distinct" assertion via `Set` size. Updated the two existing `chat-completions.test.ts` assertions that exercised these paths (one previously did a loose `.toContain()` substring match on the old generic string; one asserted only `callCount === 0` with no reason check at all) to assert the specific new constants. **Verified**: full `packages/routing` + `apps/api` suites, 201/201 passing, lint and build clean. **Not done**: no schema/route/UI changes — this is a pure code-quality/observability fix to an existing pipeline stage, not a Ship-list item.
- **2026-07-19**: Surfaced `routingMetadata.configSource` (`"request"` \| `"api_key"` \| `"workspace"` \| `"org"` \| `"platform_default"`) alongside `routing.reason`, sourced directly from `resolveRouterConfig()`'s own already-computed `source` value rather than re-derived. `RouterLLMDecision` gained an optional `configSource` field; `LiveRouterLLMService.decide()` attaches `resolved.source` to every value it returns, including its own internal `MockRouterLLMService` fallbacks (no usable provider adapter, or the resolved model unsupported) — resolution genuinely happened in those cases even though the resolved provider turned out unusable, so the source is still worth reporting. `toEngineDecision()` derives `routingMetadata.configSource` from `routerDecision.configSource`, mirroring the existing `routerModelUsed`/`routerConfidence` pattern exactly — no new mechanism invented. **`chat-completions.ts` needed zero changes**: confirmed via direct read that `routingMetadata` and `routemind.routing` in the response already reference the identical object, so the new field flows through automatically. Extended `router-config-wiring.test.ts` with 2 new cases (api_key-scoped, org-scoped) and `configSource` assertions on the 2 existing cases (workspace-scoped, platform-default) — 4 of `resolveRouterConfig`'s 5 levels now proven end-to-end via real HTTP requests against real Postgres rows. **Decision, flagged not silently skipped**: the 5th level ("request" override) still has no live HTTP path to test through, since no per-request override field exists in the `/v1/chat/completions` schema yet (explicitly deferred in two earlier slices) — it remains covered only by the pre-existing pure unit tests in `packages/routing/src/router-config.test.ts`. **Verified**: full `packages/routing` + `apps/api` suites, 203/203 passing, lint and build clean. **Checklist**: BYO Router Model's Ship-list description now reads "schema, resolver, identity plumbing, live wiring, and response-metadata surfacing all done" — CRUD routes, dashboard UI, and the per-request override field are what's left.
- **2026-07-20**: Added CRUD routes for `RouterConfig`: `POST/GET/PATCH/DELETE /v1/workspaces/:workspaceId/router-config` and the `/v1/organizations/:organizationId/router-config` equivalent (api_key-scoped explicitly deferred, per this slice's scope — org-scoped was NOT deferred, unlike an earlier exploration subagent's unsolicited suggestion to drop it, which was overridden since the user explicitly asked for both). Gated by two new permissions, `router.read`/`router.manage` (Admin+Owner only, mirroring `provider.manage`'s placement in `seed-rbac.ts`). `RouterConfig` is a singleton per scope (its own `@@unique([scopeType, scopeId])`), so POST 409s if one already exists and PATCH/DELETE 404 if none does — no `:id` path segment needed. **The explicit "don't let someone point a RouterConfig at a credential they don't own" ask**: every create/update validates the resolved `credentialId` names an enabled `ProviderCredential` actually owned by the saving scope (workspace-scoped: `credential.workspaceId` must equal the target workspace; org-scoped: the credential's workspace must belong to that org) — plus a bonus check the ask didn't name but the investigation surfaced: `credential.provider` must match the `RouterConfig`'s own `provider`, since a mismatch would otherwise silently break at runtime inside `LiveRouterLLMService`'s credential-resolution path (a mismatched-provider apiKeys map would produce a `providerFactory` lookup miss, silently falling back to `MockRouterLLMService`). **rbac.ts change, not silently made**: `requireOrganizationPermission`'s org-id extraction previously only read `request.body.organizationId` (fine for its one existing caller, `POST /v1/workspaces`) — GET/DELETE requests have no body, so it now checks the route's `:organizationId` param first, falling back to body. Also extended `RbacOrgContext` with `workspaceId` (the workspace whose Membership satisfied the org-level check), since `AuditEvent.workspaceId` is a required FK and org-scoped mutations have no workspace of their own to attach an audit event to otherwise. Added `router_config.create/update/delete` to `AuditAction` and `RouterConfig` to `AuditTargetType`; every mutation audits in the same transaction, matching the existing pattern exactly. 17 new integration tests (`router-config.test.ts`): correct-role-succeeds and incorrect-role-403 for both scopes, cross-workspace rejection, three distinct invalid-credentialId cases each with a clear message, full get/update/delete lifecycle for both scopes, the singleton 409 conflict, and audit-log attribution. **Verified**: full `apps/api` suite, 206/206 passing (189 previous + 17 new), lint and build clean. **Gotcha**: `npx vitest run apps/api` hit a transient `VirtualAlloc failed` spawning worker processes (an OS-level resource issue, not a real failure) — resolved with `--no-file-parallelism` on retry, worth remembering if this recurs. **Not done**: api_key-scoped CRUD, dashboard UI for managing `RouterConfig` rows. **Checklist**: BYO Router Model's Ship-list description now includes workspace/org CRUD routes as done.
- **2026-07-20**: The ask ("build the RouterConfig dashboard UI, with an org-level section, and surface `configSource` in the requests/logs view") turned out to bundle 4 separate slices, two blocked on infrastructure that doesn't exist at all yet — investigated and reported before writing anything, per this skill's "propose splitting" instruction, rather than attempting all of it: **(1)** workspace-scoped RouterConfig UI — buildable now. **(2)** an org-level section — blocked on zero existing org-context infrastructure in the dashboard (no `GET /v1/organizations/:id/me`, no org switcher, no way for the UI to even know which org a workspace belongs to). **(3)** org-scoped RouterConfig UI — depends on (2). **(4)** `configSource` visibility in the Requests view — blocked on the Requests page being a pure demo-data stub with zero real backend wiring, and `configSource` never being persisted to any queryable log store (it only exists in the live HTTP response). User picked (1). Shipped: a new "Router Config" nav item (gated `router.read`, greyed out below that) leading to a workspace-scoped screen — view/create/edit/delete a workspace's `RouterConfig`, following `members/page.tsx`'s RBAC-gated pattern exactly (`canManage`/`permissionRequirementLabel` disabling every mutating control). **Real gap found and closed, not scope creep**: the credential picker needed a backend list endpoint that didn't exist — added `GET /v1/workspaces/:workspaceId/provider-credentials` (metadata only, never `encryptedApiKey`), gated on `provider.manage` to match `router-config.ts`'s own ownership check. The picker filters to credentials matching the selected provider client-side, steering users away from the provider-mismatch 400 rather than letting them hit it blind. **Decision**: `getRouterConfig()` deliberately doesn't throw on 404 — "no config yet" is the normal, common state for this singleton-per-workspace resource, not an error worth a page banner. Added `router.read`/`router.manage` to the dashboard's `PERMISSION_MIN_ROLE` tooltip map (both "Requires Admin", matching the backend taxonomy). 6 new integration tests for the credential-list endpoint (role gate, cross-workspace rejection, disabled-credentials excluded, no secret leakage). **Verified**: full `apps/api` suite 212/212 passing, dashboard `tsc --noEmit` clean, full `next build` clean (hit the same transient `spawn UNKNOWN`/`VirtualAlloc` OS resource exhaustion seen in a prior slice's vitest run — this machine seems to accumulate stray node processes across sessions; resolved on retry both times, worth a `/db-migration-check`-style housekeeping pass on stray processes if it keeps recurring). **Not done, explicitly deferred to the next two slices**: org-level dashboard foundation + UI, and wiring `configSource` into a real, persisted Requests view.
- **2026-07-20**: Started the declarative policy engine — the last of Phase 0's originally-listed Ship items to get going. Added `Policy` (migration `20260720075525_add_policy`): `workspaceId` (real FK, unlike `RouterConfig`'s polymorphic `scopeId` — the task's own spec asked for `workspace_id` directly), `subjectType` (`role`\|`user`\|`api_key`), `subjectId`, `ruleType` (`model_restriction`\|`cost_cap`\|`budget`), `ruleJson`, `priority`. `evaluatePolicy()` in `packages/policy-engine` follows `resolveRouterConfig()`'s established pattern exactly — a pure function taking an injected `PolicyRuleLookup` instead of a database client, unit-testable without Prisma. **Investigated before touching anything, per "absorb existing functionality, not add a parallel system"**: the exploration for this slice initially flagged the existing `PolicyRule` model as an unused stub safe to replace — directly grepped for it myself and found it's actually **live**, backing the unrelated prompt-firewall feature's secret/PII/prompt-injection detection rules (`prompt-firewall-service.ts`). Left it completely untouched; it's a different domain that happens to share a similar name, not a parallel system to this one — a second instance this session of a subagent's confident "this is dead/safe to remove" claim being wrong, worth continuing to verify rather than trust at face value. **Semantics decided, not left ambiguous**: rules only ever produce an explicit deny when their condition genuinely applies to the request (wrong model for a restriction, cost/spend over a cap) — a rule that doesn't apply is skipped, not treated as an allow-match; a request nothing denies falls through to allow-by-default. Multiple applicable rules are evaluated in priority order (highest first, ties broken by earliest-created), returning the first rule that actually matches and denies. The `budget` rule type's math (`projectedSpend = currentSpendUsd + estimatedCostUsd`, deny if it exceeds `maxSpendUsd`) deliberately reproduces `CostGuardrailService`'s existing hardcoded threshold exactly, proven by a dedicated test — so the eventual live-path swap lands on an already-verified-equivalent replacement instead of needing a redesign then. 9 unit tests: model_restriction blocks/passes-through, cost_cap blocks/passes, budget blocks with the CostGuardrailService-equivalent math, no-match allows by default (including cross-workspace isolation), priority ordering across multiple matching rules, and a higher-priority non-matching rule correctly falling through to a lower-priority one that does match. **Verified**: `packages/policy-engine` build clean, 9/9 new tests passing, full `apps/api` suite (212 tests) unaffected/still green — confirming no live behavior changed, matching this slice's explicit "don't wire into the live request path yet" scope. **Not done, deferred to a future slice**: `CostGuardrailService`/`chat-completions.ts` still use the old hardcoded `UserBudget`/`UsageQuota` checks unchanged; no CRUD routes or dashboard UI for managing `Policy` rows; `model_restriction`/`cost_cap` rule types have no live consumer yet either. **Checklist**: declarative policy engine moves from "not started" to "schema + evaluatePolicy done" — Phase 0 is not complete: this still needs live wiring, and the audit-log/RBAC coverage gaps on `onboarding.ts` flagged since 2026-07-10 remain open too.
- **2026-07-20**: Wired `evaluatePolicy()` into `CostGuardrailService`'s workspace-scoped budget check (commit `1aeb402`), replacing the hardcoded `UserBudget.maxSpendUsd` comparison with a live call through a new `PrismaPolicyRuleLookup` adapter, using the already-proven-equivalent `budget` rule type from the previous slice. Resolves the caller's `roleId` (from `workspaceRole` on `AuthenticatedUser`, capitalized to match `Role.name`) and evaluates against `Policy` rows for that workspace — `chat-completions.ts` now threads `apiKeyId`/`workspaceRole` through to `checkBeforeRequest` for this purpose. **Legacy path preserved on purpose**: the workspace-less (`!workspaceId`) threshold check is unchanged, since `Policy.workspaceId` is a required FK with no equivalent for that pre-tenancy case — same boundary this project has drawn around `onboarding.ts` since 2026-07-10. **No production data to migrate**: grepped for `UserBudget` writes and found only `cost-guardrail-service.ts`'s own `updateMany` and one test fixture — no live route creates `UserBudget` rows, so "migrate existing hardcoded budget config into Policy rows" reduced to updating that one fixture, not a real backfill. **Real bug found and fixed, required for testability, not scope creep**: `seedWorkspaceWithRole` (the shared RBAC test fixture) only ever created an RBAC `Membership` row, never the legacy `WorkspaceMember` row that `PrismaApiKeyAuthenticator` actually reads `workspaceRole` from — so `AuthenticatedUser.workspaceRole` was always `undefined` for every fixture-created user, silently breaking role-based `Policy` matching before it could ever be tested. Fixed by creating both rows in sync (mirroring what the real invite-acceptance flow already does); nothing else depended on `workspaceRole` being set before now, confirmed via grep. New `cost-guardrail-policy.test.ts` (2 tests): one proves the live path is decided by `evaluatePolicy()` specifically (a near-zero `Policy` threshold blocks a request despite an enormous legacy `UserBudget.maxSpendUsd`, and `currentSpendUsd` stays untouched confirming the block happened before any provider call); one reproduces pre-swap block/allow equivalence. `phase0-isolation.test.ts` updated to seed a matching `Policy` row alongside its existing `UserBudget` row per workspace fixture, so budget isolation keeps exercising real enforcement post-migration instead of silently becoming a no-op. **Verified**: full `apps/api` suite, 214/214 passing (212 previous + 2 new), lint clean, build clean. **Not done, deferred**: no CRUD routes or dashboard UI for `Policy` rows; `model_restriction`/`cost_cap` rule types still have no live consumer (only `budget` is wired); `onboarding.ts` remains unaudited/un-RBAC'd, same long-standing boundary. **Checklist**: declarative policy engine now reads "schema + evaluatePolicy() + live wiring done" — Phase 0's two originally-unstarted Ship items (BYO Router Model, declarative policy engine) have both now reached live-wiring status; remaining gaps across both are CRUD/UI/observability polish, not core mechanism, plus the still-open `onboarding.ts` RBAC/audit boundary from 2026-07-10.
- **2026-07-20**: Closed the `onboarding.ts` RBAC/audit boundary flagged since 2026-07-10 (commit `19cc726`). **Investigated first, per explicit instruction**: `onboarding.ts` turned out not to be the signup flow at all (that's `auth.ts`'s magic-link path, already audited) — it's a separate, still-live, publicly documented (README quickstart) and SDK-exposed flat CRUD surface with a real IDOR vulnerability, not just a missing audit trail: every route past user creation trusted a body-supplied `userId` with zero proof of ownership (mint API keys for any user, attach credentials to any user, toggle any credential by id, grant/revoke any user's model access). **Presented the tradeoff to the user before fixing** (a real fix requires authenticating callers, which breaks the documented unauthenticated quickstart) — user chose: `POST /v1/users` and a user's first `POST /v1/api-keys` stay unauthenticated (true bootstrap); every other mutation now requires the caller's own API key via `X-API-Key`, checked against the `userId` named in the body/params (`PATCH /v1/provider-credentials/:id` returns an identical 404 for "not found" and "not yours," so ownership can't be probed). No SDK signature change needed — `RouteMindClient` already attaches its configured `apiKey` to every request; only bare curl examples were missing it. **Blocker found mid-implementation, resolved without re-asking**: `AuditEvent.workspaceId`/`.principalId` are required FKs, and onboarding-created users had neither a workspace nor a principal (`createUser` only ever created a bare `User` row) — nothing to audit against. Fixed by having `PrismaOnboardingStore.createUser` provision a real `Organization`/`Workspace`/`Membership` in one transaction, mirroring `auth.ts`'s self-serve-signup pattern exactly (`personal-<userId>` slug, matching the Phase 0 backfill script's convention). Every subsequent mutation (`createApiKey`, `createProviderCredential`, `updateProviderCredential`, `upsertModelAccess`) now resolves that workspace context, sets `workspaceId` on the created row (closing a `CLAUDE.md` workspace_id violation these rows had, as a side effect of needing it for audit anyway), and writes an `AuditEvent` in the same transaction. Two new audit actions (`provider_credential.upsert`, `provider_credential.update`), one new (`model_access.upsert`), two new target types (`ProviderCredential`, `UserModelAccess`). `InMemoryOnboardingStore` (the fast test fixture) deliberately did NOT gain workspace/audit modeling — audit correctness is verified separately in a new Prisma-backed suite (`onboarding-audit.test.ts`, 4 tests) since the in-memory store can't represent it; the existing fast suite (`onboarding.test.ts`) gained 8 new IDOR/ownership tests (cross-user rejection on credentials/model-access/available-models, second-API-key-requires-auth, PATCH ownership 404). **Verified**: full `apps/api` suite 224/224 passing, lint clean, build clean. **Not done, explicitly deferred**: backfilling `workspaceId` onto pre-existing onboarding-created rows in production (stays `NULL` until a dedicated backfill slice); unifying onboarding.ts's unverified-email signup with auth.ts's magic-link signup — they remain two separate paths, that decision was never asked for here. **Checklist**: Audit log's onboarding.ts caveat, open since 2026-07-10, is closed.
- **2026-07-20**: Built CRUD routes and a dashboard screen for `Policy` rows (commit `b0e15e8`), the last item keeping the declarative policy engine's Ship-list entry incomplete. Workspace-scoped `POST/GET /v1/workspaces/:workspaceId/policies` and `PATCH/DELETE .../policies/:policyId`, gated by new `policy.read`/`policy.manage` permissions (Admin+Owner, same placement as `router.read`/`router.manage`). **No org-scoped variant, unlike RouterConfig**: `Policy.workspaceId` is a plain required FK, not a polymorphic `scopeType`/`scopeId` pair, so there was nothing to mirror there — confirmed against the schema before assuming RouterConfig's exact shape applied. **The explicit "validate rule_json against the shape evaluatePolicy() expects" ask**: `evaluateRule()` reads `ruleJson` fields off an untyped cast and silently treats missing fields as "rule doesn't apply, not a match" — so a malformed save wouldn't error at evaluation time, it would just silently never enforce anything, which is worse than an upfront rejection. Added a per-`ruleType` Zod schema (`blockedModels: string[]` / `maxCostUsd: number` / `maxSpendUsd: number`) mirroring `packages/policy-engine`'s own interfaces exactly, checked before every create and before any `ruleJson` update. **Also added, not explicitly asked but the same failure class**: `subjectId` ownership validation — `evaluatePolicy()` matches `subjectId` by exact equality against the caller's own `roleId`/`userId`/`apiKeyId`, so a foreign or typo'd `subjectId` would likewise silently save as a rule that can never match anything; now `role` must be an existing `Role.id`, `user` must be a member of the target workspace, `api_key` must belong to the target workspace, mirroring `RouterConfig`'s `credentialId` ownership check from an earlier slice. PATCH only accepts `ruleJson`/`priority` — changing `subjectType`/`subjectId`/`ruleType` requires delete+recreate, since a type change would need entirely different validation logic than an update path is set up for. All three mutations audited (`policy.create`/`update`/`delete`, target type `Policy`) in the same transaction as the row change. **Gap found and closed for the UI, not scope creep**: no endpoint exposed the seeded `Role` id/name list, which the dashboard's role-subject picker needs to resolve a role NAME to the `Role.id` `evaluatePolicy()` actually matches against — added `GET /v1/workspaces/:workspaceId/roles` (`policy.read`-gated; roles themselves are global reference data, the `:workspaceId` in the path is purely for identity resolution, same as every other RBAC route). Dashboard: new `/dashboard/policies` screen follows `members/page.tsx`'s list-resource pattern (create form + table), not `router-config`'s singleton form, since Policy is a list — subject-type-dependent subject picker (backed by the new roles endpoint, the existing members endpoint, and the existing workspace api-keys endpoint), rule-type-dependent input fields, inline priority edit via table row, delete. Same `canManage`/`permissionRequirementLabel` RBAC-gated-control pattern as every other screen; new nav item gated `policy.read`. 19 new integration tests (`policy.test.ts`): role-gated create/reject, cross-workspace rejection, all three `ruleJson` shape-mismatch cases, all three `subjectId` ownership cases (reject + accept for user and api_key, reject for role), list ordering (priority desc, createdAt asc), cross-workspace list isolation, PATCH behavior (update, reject wrong-shape `ruleJson`, 404 cross-workspace), delete + audit attribution, plus the new roles-list endpoint. **Verified**: full `apps/api` suite 243/243 passing (224 previous + 19 new), dashboard `tsc --noEmit` and full `next build` clean, lint clean. **Checklist**: declarative policy engine's Ship-list entry now reads "schema + evaluatePolicy() + live wiring + CRUD routes + dashboard UI all done." **Not done, deferred**: `model_restriction`/`cost_cap` rule types have no live enforcement consumer (only `budget` is wired into `CostGuardrailService`) — rows of those types can be fully managed through this new UI but don't yet block any request; wiring them in is a separate slice, not attempted here.
- **2026-07-21**: Wired `model_restriction` and `cost_cap` into live enforcement (commit `3dcbe50`), closing the gap flagged at the end of the previous slice. **Real bug found while wiring, not scope creep**: `evaluatePolicy()` in `cost-guardrail-service.ts` was only ever called inside `if (budget && input.workspaceId)` — a workspace whose only `Policy` rows were `model_restriction`/`cost_cap`, with no `UserBudget` row at all, got zero policy evaluation, silently. Fixed by evaluating unconditionally for any workspace-scoped request (`currentSpendUsd` falls back to `0` when no budget row exists, which is correct for rule types that don't care about spend). `requestedModel` is now threaded in from `chat-completions.ts`'s existing per-fallback-candidate loop — the same place the `budget` rule already got checked, once per candidate — so a `model_restriction` denial on the primary candidate rejects the whole request immediately, identical to existing `budget` behavior on the primary candidate. **Also fixed**: every policy denial was hardcoded to `errorCode: "BUDGET_EXCEEDED"` regardless of which rule type matched — a `cost_cap`/`model_restriction` block would have been silently mislabeled. Added distinct `MODEL_RESTRICTED`/`COST_CAP_EXCEEDED` codes, mapped from `policyResult.matchedRule.ruleType` in a new `buildPolicyDenial()` helper. **Dashboard "not yet enforced" warning deliberately not built**: the ask wanted one only until both rule types were wired, and this slice wires both in full — a warning claiming they're unenforced would be wrong the moment this shipped, so building it would have been actively misleading; flagged rather than silently skipped. 4 new tests in `cost-guardrail-policy.test.ts`: `model_restriction` blocks a listed model (with no `UserBudget` row at all, proving the unconditional-evaluation fix) and allows an unlisted one; `cost_cap` blocks a request whose estimated cost exceeds `maxCostUsd` and allows one within it — each asserts the specific new error code, not just the status code. **Verified**: full `apps/api` suite 247/247 passing (243 previous + 4 new), lint clean, build clean. **Flagged for a future slice, not acted on**: `evaluatePolicy()` now runs on every workspace-scoped request regardless of whether a budget row exists, adding a `Policy` lookup per fallback candidate for workspaces that previously skipped it entirely — correct, but a caching layer is worth revisiting if it shows up in latency profiling. **Checklist**: the declarative policy engine's last open item ("model_restriction/cost_cap have no live consumer") is closed — all three rule types are now fully wired end to end.
- **2026-07-21**: Report-only slice, per explicit "don't fix anything found" instruction (commit `ce85774`) — re-ran `npm run qa:phase0 -w apps/api` and extended the Phase 0 exit-criteria test to cover the two features that shipped since it was last written (RouterConfig, and `model_restriction`/`cost_cap`). **`qa:phase0` result: 8 PASS, 0 GAP, 0 FAIL** — identical to the last run, no regression. **`phase0-isolation.test.ts` previously only proved isolation for Membership/roles, ProviderCredential, and a single `budget`-type Policy row** — confirmed via direct read, not assumed from memory. Extended the existing four-workspace/two-org fixture with a distinct `RouterConfig` per workspace (reusing each workspace's own already-seeded `ProviderCredential`) and all three `Policy` rule types per workspace, with `model_restriction`/`cost_cap` values deliberately made workspace-specific (not a shared constant like the budget rows) so a blocking rule on one workspace could be proven not to leak into a different workspace's otherwise-identical request. 4 new tests: RouterConfig rows are distinct and not shared; workspace A1 cannot read workspace B's router config (403, with a positive control proving A1 can read its own); workspace A2's `model_restriction` policy blocks A2's own `gpt-4o` request without blocking workspace A1's identical request; workspace B1's `cost_cap` policy blocks B1's own request without blocking workspace B2's identical request. **Verified**: full `apps/api` suite 251/251 passing (247 previous + 4 new), lint clean, build clean, `check:orphaned-workspaces` clean (9 scanned, 0 orphaned) confirming the QA script's own cleanup held. **Result of the whole slice: no gaps or failures found anywhere** — nothing was fixed because nothing broke. **Checklist**: Phase 0's exit-criteria demo now explicitly covers RouterConfig and all three Policy rule types, not just budget; Phase 0 itself is still not marked fully complete since BYO Router Model's Ship-list entry still has explicitly-deferred sub-items (api_key-scoped CRUD, org-level dashboard UI, `configSource` in a real Requests view, a per-request override field) that this slice didn't touch.
- **2026-07-22**: Built a real Playwright e2e report generator (commit `eb28053`) — `apps/api/src/scripts/e2e-report-generator.ts` + `e2e-report-html.ts`, run via `npm run qa:e2e-report -w apps/api`. Unlike `qa-phase0-dashboard-flow.ts` (which drives the real Fastify app in-process via `app.inject()`), this drives an actual Chromium browser via Playwright against the real `apps/api`/`apps/dashboard` dev servers, plus direct `fetch` calls for the endpoints the task asked to see recorded raw, and renders everything into one self-contained HTML file (embedded base64 screenshots, raw request/response JSON) under `reports/` (new, gitignored). Covers: signup magic-link (empty form → "check your email" → real `/v1/auth/verify` call, not a UI shortcut → landing in the default workspace), creating a second workspace, inviting + accepting as a Developer teammate in a separate browser context, a gated-action attempt, service-account creation/key-issue/key-revoke, the audit log, a workspace-scoping negative-case check (`router-config`/`policies` cross-workspace calls correctly 403), and a real `/v1/chat/completions` call showing `routing.reason`/`configSource`. **Design decision, not incidental**: the script spawns its own dedicated api+dashboard dev-server processes (refusing to start if ports 3000/3002 are already occupied) specifically so it can capture magic-link tokens from `ConsoleEmailSender`'s stdout — `LoginToken`/`WorkspaceInvite` only ever store a hash of the token, so there is no way to recover the real token from the database after the fact once the email is "sent." **Two real bugs found and fixed in the script itself, not the product, during development**: (1) Windows requires `shell: true` to spawn `npm.cmd`, and `proc.kill()` alone only kills that shell wrapper, not the underlying npm/tsx/next process tree — fixed with `taskkill /T /F` on Windows. (2) The extracted session cookie is a full `"name=value"` pair (correct for a raw `Cookie:` header) but Playwright's `context.addCookies()` wants only the value half — passing the whole pair silently broke every route that actually calls `verifySessionToken()` (401, not 403) while routes with no auth gate at all (like `GET .../members`, itself worth independently noting) kept "working," which is what made the bug non-obvious at first. **Two real product gaps surfaced by actually using a browser, documented in the generated report rather than fixed here, per this slice's explicit scope**: revoking a service-account key (`DELETE .../service-accounts/:id/keys/:keyId`) is blocked by the browser's own CORS preflight check (`Method DELETE is not allowed by Access-Control-Allow-Methods`) — this can only ever be caught by a real browser, never by `app.inject()`-based tests, which bypass CORS entirely; and `"gpt-4o"` returns `400 Unsupported model` in the default mock provider mode. Also documented, not fixed: no workspace-switcher UI exists anywhere in the dashboard (confirmed via `app-shell.tsx`'s static "Workspace: Default" label) — the report shows the actual mechanism in its place (flipping the `routemind_active_workspace` localStorage entry) rather than a UI that doesn't exist. **Verified**: full `apps/api` suite 251/251 passing (unchanged, confirming this added no product-code changes), lint and typecheck clean, `check:orphaned-workspaces` clean after the run (9 scanned, 0 orphaned), and the script itself completed cleanly end-to-end multiple times with `reports/*.html` produced each time. **Not done**: the two documented product gaps (CORS-blocked key revocation, `gpt-4o` model support) were intentionally left unfixed, per explicit instruction to report reality rather than fix it — worth their own slices if they matter.

---

## Phase 1 — Productization

**Theme: turn a feature set into something a stranger can buy**
**Timeframe: ~6–8 weeks | Engineering + first business decisions**

This is where the pricing model from our earlier conversation becomes real, and where the "transparent, predictable pricing" USP either becomes true or stays a slogan.

**Ship:**

- `Plan`, `Subscription`, `UsageSummary` schema; Stripe Billing integration (subscriptions + metered overage)
- Plan-gated feature flags evaluated through the same policy-engine contract as everything else (not scattered `if` checks)
- Self-serve signup flow: org creation → default workspace → first API key, with zero manual steps from you
- Model discovery/sync (Proposed Feature 4) — this directly reduces time-to-first-successful-request, which matters enormously for self-serve conversion
- Workspace/API-key-scoped dashboard (falls out cheaply now that RBAC exists)
- Public docs site + pricing page that states real numbers, not "contact sales" for every tier

**Recommended plan structure** (from the pricing conversation, now made concrete):

- **Free**: 1 org, 1 workspace, rule/score-based routing, 10k requests/month — pure adoption
- **Pro** (flat monthly): multi-workspace, BYO router model, prompt firewall, basic RBAC
- **Team**: service accounts, policy engine, audit log, scoped dashboards
- **Enterprise**: custom — SSO, dedicated infra, compliance

**Exit criteria:** a person who has never spoken to you can sign up, hit the Free tier's request limit, see a clear upgrade prompt, and pay with a credit card, end to end, with no manual intervention.

**Why this order:** you cannot validate the BYO-Everything or transparent-pricing USPs with real customers until people can actually pay you. Building more differentiated features before this is optimizing a product nobody can buy yet.

### Session log

- **2026-07-22**: Shipped `docs/phase-1-plan.md` item 1 (`Plan`/`Subscription` schema, schema + seed only, no Stripe wiring). Added `Plan(id, name, priceCents, includedRequests, featuresJson, createdAt, updatedAt)` and `Subscription(id, organizationId, planId, status, stripeSubscriptionId, currentPeriodStart, currentPeriodEnd, createdAt, updatedAt)` to `schema.prisma` (migration `20260722062830_add_plan_subscription`), plus `apps/api/src/scripts/seed-plans.ts` (`npm run seed:plans -w apps/api`). **Real pricing, not a placeholder**: asked before seeding, since the public pricing page (item 8) will read these numbers directly — Free $0/mo (10k requests), Pro $59/mo (100k), Team $199/mo (1M), Enterprise contact-sales (`includedRequests: null`, `featuresJson.contactSales: true`). **Design decisions, flagged not silently made**: `Subscription.organizationId` is `@unique` (one active subscription per org, not billing history — matches the phase item's literal `Subscription(org_id, ...)` org-level scoping, not workspace-level); `Plan` has no `workspaceId` since it's platform-level reference data, same category as `Role`/`RolePermission`, not tenant-owned; `stripeSubscriptionId`/`currentPeriodStart`/`currentPeriodEnd` are nullable until item 2 wires real Stripe Billing; no `Subscription` row is backfilled for existing Organizations, on purpose (schema + seed only, per this item's explicit scope — item 2 or a dedicated slice decides what a pre-existing org's default subscription should be). `seedPlans()` deliberately upserts with a real `update` clause (not `seed-rbac.ts`'s create-only `update: {}`), since pricing is far more likely to be revised before go-live than the RBAC taxonomy is — confirmed idempotent by running it twice and querying directly (4 rows, no duplicates). **Real bug found and fixed in the seed script itself**: the log line's `includedRequests.toLocaleString()` rendered `100,000` as `1,00,000` (Indian digit-grouping) because it picked up this machine's default ICU locale instead of a fixed one — fixed by passing `"en-US"` explicitly; cosmetic only, never touched the stored numbers. **`db-migration-check` run**: schema diff confirmed, migration folder present, no `packages/shared/src/types` update needed (same established precedent as `Policy`/`RouterConfig`/`Organization` — not exported there either). **Verified**: full `apps/api` suite 251/251 passing (unchanged), lint and typecheck clean. **Not done, explicitly deferred**: no API routes, no dashboard UI, no Stripe fields populated — those are items 2 and 5. **Next**: item 2, Stripe integration for subscription lifecycle (checkout session creation, webhook handling) — the first item that needs real Stripe test-mode credentials, worth flagging before starting since none exist in this environment yet.
- **2026-07-23**: **Scope change, decided before this slice, not during it**: item 2 was re-specified to use Paddle Billing instead of Stripe — no Stripe code existed yet (item 1 only ever had a placeholder `stripeSubscriptionId` column), so this was a clean swap, not a rip-and-replace. Shipped: `Subscription.stripeSubscriptionId` renamed to `paddleSubscriptionId` + a new `paddleCustomerId` (migration `20260723051709_paddle_billing_fields`), `Plan.paddlePriceId` (nullable — null for Free/Enterprise), `PADDLE_ENVIRONMENT`/`PADDLE_API_KEY`/`PADDLE_CLIENT_TOKEN`/`PADDLE_WEBHOOK_SECRET` in `config.ts` (the first three required in production only if `PADDLE_ENVIRONMENT=production`), a native-`fetch`-based `LivePaddleClient` (`infrastructure/paddle-client.ts` — deliberately no Paddle SDK dependency, matching `packages/providers`' existing style), a hand-rolled `verifyPaddleWebhookSignature()` (`infrastructure/paddle-webhook.ts`, HMAC-SHA256 + `timingSafeEqual` over `${ts}:${rawBody}`, mirroring `session.ts`'s existing crypto convention instead of pulling in a signing library), and `apps/api/src/routes/billing.ts`: `POST /v1/organizations/:organizationId/billing/checkout` (new `billing.manage` permission, Owner-only, via `requireOrganizationPermission`) and `POST /v1/webhooks/paddle` (signature-verified, not RBAC-gated) handling `subscription.created/updated/canceled`. **Real bug found while writing this, not in the new code's logic but in Prisma's own comment-to-JSDoc translation**: a `///` schema comment from the previous slice containing the literal substring `*/` (`currentPeriod*/paddle*`) prematurely closed the generated `.d.ts`'s JSDoc block, corrupting the entire generated Prisma client and producing dozens of unrelated-looking TS parse errors — fixed by rewording the comment; worth remembering that `*/` anywhere in a Prisma doc-comment silently breaks codegen. **Real Fastify pattern introduced fresh, no prior precedent in this codebase**: the webhook route needs the raw request body for signature verification, but Fastify's default JSON parser is global — solved by registering the webhook route inside its own encapsulated `app.register()` plugin scope with a scoped `addContentTypeParser("application/json", {parseAs:"buffer"}, ...)`, so only that one route sees raw bytes; every other route keeps normal JSON parsing untouched. **Design decisions, flagged not silently made**: the checkout call to Paddle and the audit-event write are two separate operations (not one `$transaction`) since the external HTTP call can't be rolled back by our database anyway; webhook-driven `Subscription` changes are not audited (no principal exists for a Paddle-initiated event, and inventing a "system principal" concept was judged out of scope for this slice); no `PaddleWebhookEvent` dedup/replay-ledger table — the sync is upsert-based and naturally idempotent, so a replayed webhook just rewrites the same fields rather than corrupting anything; unprocessable webhook payloads (no matching org/plan) are logged and acknowledged with 200 rather than erroring, since retries can't fix a permanent data-shape mismatch. **Verified**: 10 new tests in `billing.test.ts` (checkout success + audit attribution, 403 for non-Owner, 403 cross-org, 404 unknown plan, 400 no-`paddlePriceId` plan, invalid-signature 401, all three webhook event types syncing `Subscription` correctly, unknown event type acknowledged), full `apps/api` suite 261/261 passing (251 previous + 10 new), lint and typecheck clean, `db-migration-check` clean. **Not done, explicitly deferred to item 5**: no dashboard UI, no actual `Paddle.js`/`Paddle.Checkout.open()` browser-side call — this slice only exposes the backend contract (transaction id + client token) that item 5's UI will consume. **Not verified against Paddle's real sandbox**: no live Paddle credentials exist in this environment, so `LivePaddleClient`/the webhook signature scheme are implemented to match Paddle Billing's documented API/webhook format as accurately as possible, but have not been exercised against an actual Paddle sandbox account — worth a real smoke test the first time real sandbox credentials are available. **Checklist**: item 2 is done (schema + checkout + webhook); item 5 (billing UI) is next in sequence but item 3/4 (usage metering, plan-limit enforcement) remain unstarted and unordered relative to it in the checklist.
- **2026-07-23**: Shipped item 3, usage metering. **Investigated first, per the item's own "reuse existing RequestLog/AnalyticsEvent data" instruction**: there is no `AnalyticsEvent` model anywhere in this codebase (confirmed via grep) — only `RequestLog` carries token/request data; the phase plan's wording was a loose reference to the general idea, not a literal second table. `RequestLog` also has no `organizationId` of its own, only `workspaceId`, so aggregation joins through `Workspace.organizationId`. **On-read vs. nightly job, decided by measuring, not guessing**: queried local dev Postgres directly — 19 `RequestLog` rows total — and confirmed via grep that no job-scheduling infrastructure (cron/queue library) exists anywhere in `apps/api`. Given that, chose on-read computation with no persisted `UsageSummary` table at all: `infrastructure/usage-summary-service.ts`'s `computeUsageSummary()` runs a parameterized `$queryRaw` (`RequestLog` JOIN `Workspace`, filtered by `organizationId` + period, `status = 'success'` only) fresh on every call — a persisted table would itself have been "a second counting system," which the item explicitly said not to build. New `GET /v1/organizations/:organizationId/billing/usage-summary` route (`billing.ts`), gated by a new `billing.read` permission (Admin+Owner, mirroring the existing `X.read`/`X.manage` pairing already used for `router`/`policy`). Defaults to the org's current `Subscription.currentPeriodStart/End` if one exists, else the current UTC calendar month (`defaultUsagePeriod()`) — explicit `periodStart`/`periodEnd` query params override both. **Interpretation decision, flagged not silently made**: only `status = "success"` `RequestLog` rows count toward `requestCount`/`tokenCount` — a request that failed before reaching a provider was never actually billable usage; this wasn't spelled out in the item's literal spec. **Verified**: 5 new tests in `billing.test.ts` (correct sum scoped to org + period + success-only, default-period fallback, `billing.read` RBAC gate on Viewer vs Admin, cross-org rejection), full `apps/api` suite 266/266 passing (261 previous + 5 new), lint and typecheck clean. **Not done**: no dashboard UI (not asked for by this item); no caching/precomputation — revisit only if `RequestLog` volume growth makes the on-read query show up in latency profiling. **Checklist**: items 1-3 done; items 4 (plan-limit enforcement, the natural consumer of this usage-summary computation) and 5 (billing UI) remain, in either order.
- **2026-07-23**: Shipped item 4, plan-limit enforcement. **Investigated an initial exploration report's architectural suggestion before implementing, per the standing "verify subagent claims rather than trust at face value" lesson**: the explorer's report proposed bypassing `evaluatePolicy()` entirely with a separate org-level check in `CostGuardrailService` — directly contradicts the item's explicit "same engine... do not build a parallel enforcement path" instruction, so read `packages/policy-engine/src/index.ts` and `apps/api/src/infrastructure/policy-rule-lookup.ts` myself before designing around it. **Real design decision, not obvious from the item's one-line spec**: `plan_limit` is fundamentally different from `budget`/`model_restriction`/`cost_cap` — those three are admin-configured (a human creates a real `Policy` row via the CRUD routes), but `plan_limit`'s value comes from `Plan.includedRequests`, which nothing should let a workspace admin override away. Resolved by having `PrismaPolicyRuleLookup.findRulesForWorkspace()` _synthesize_ a `plan_limit` rule on every call (never stored in the `Policy` table) by looking up the workspace's org, its `Subscription`+`Plan` (or falling back to the seeded "Free" plan by name if the org has no `Subscription` row — Free never goes through checkout, matching the exit criteria's "hit the Free tier's request limit"), and reusing item 3's `computeUsageSummary()` for the current count — still one single call path through `evaluatePolicy()`, satisfying "same engine, no parallel path" while keeping `evaluatePolicy()` itself pure/framework-free (the synthetic rule's `ruleJson` carries pre-computed `{maxRequests, currentRequestCount}`, mirroring exactly how `budget`'s `currentSpendUsd` is caller-supplied context rather than computed inside the engine). Added a new `"workspace"` `PolicySubjectType` (`subjectMatches()` returns `true` unconditionally for it) since `plan_limit` has no role/user/api_key to scope to. New `PLAN_LIMIT_EXCEEDED` error code via `buildPolicyDenial()`, same pattern as the other three. **No changes needed to `chat-completions.ts`'s call site or `CostGuardrailService.checkBeforeRequest()`'s public signature at all** — organizationId is derived internally from workspaceId inside the lookup (a join it already needs to make), which turned out simpler and lower-risk than the explorer's suggested design of threading a new field through multiple layers. **Verified**: 3 new pure unit tests in `packages/policy-engine` (blocks at the limit, allows under it, the new `"workspace"` subject type applies with no role/user/apiKey context at all) and 4 new live-wiring tests in `cost-guardrail-policy.test.ts` (blocked at the limit, allowed under it, never blocked when `includedRequests` is `null`/unlimited, the Free-plan fallback with no `Subscription` row) — full suite 282/282 passing (`apps/api` 266 previous + 4, `packages/policy-engine` 9 previous + 3), lint and typecheck clean (had to rebuild `packages/policy-engine`'s `dist/` output after editing its source — `apps/api` imports the built package, not raw source, so a stale `dist/index.d.ts` briefly produced confusing "no overlap" TS errors before the rebuild). One test flaked with a 5s timeout when run as part of the full monorepo suite under heavy parallel load, then passed cleanly both times run in isolation and in a second full-suite run — consistent with this machine's already-documented resource-contention pattern (`VirtualAlloc`/`spawn UNKNOWN` in earlier sessions), not a logic bug. **Not done**: no dashboard warning/UI for an org approaching its limit (that's item 5's territory). **Checklist**: items 1-4 done; item 5 (billing UI) is next — the last item before Phase 1's remaining, lower-priority items (6-8).
- **2026-07-24**: Shipped item 5, self-serve billing UI. **Investigated first, given a much earlier session (2026-07-20) explicitly flagged that org-level dashboard work was blocked on missing org-context infrastructure**: confirmed via a fresh explorer pass that this was still genuinely true — the dashboard's `ActiveWorkspace`/`VerifyLoginTokenResult`/`GET /v1/workspaces/:id/me` all carried `workspaceId` but never `organizationId`, and no dashboard code called any `/v1/organizations/...` endpoint yet. Rather than treat this as a separate blocking item, closed it as part of this slice (it's a 3-line, purely-additive backend change, not the kind of infrastructure gap that justified deferring RouterConfig's org UI in July): extended `GET /v1/workspaces/:id/me` with `organizationId` (one extra `Workspace.findUnique` alongside the two already-parallel queries) — free for every future org-scoped screen, since `usePermissions()` already fetches this endpoint on every workspace-scoped page load. Also added `GET /v1/plans` (public, unauthenticated — pricing isn't tenant-scoped secret data) and `GET /v1/organizations/:id/billing/overview` (`billing.read`-gated; combines current Subscription+Plan+usage into one response, reusing item 3/4's `computeUsageSummary()`/Free-plan-fallback logic exactly) — the billing screen needed both and neither existed. New `/dashboard/billing` page: current-plan/usage/subscription-status metric cards, an available-plans grid with upgrade buttons that call item 2's checkout endpoint then dynamically load Paddle.js and open its overlay (`Paddle.Environment.set`/`Paddle.Initialize`/`Paddle.Checkout.open`), Enterprise (or any future `featuresJson.contactSales` plan) rendered as a mailto link instead of a checkout button. New "Billing" nav item (`billing.read`-gated) and `billing.read`/`billing.manage` added to the dashboard's permission-tooltip map, matching every prior RBAC-gated screen's pattern exactly. **Verified**: 4 new backend tests (`billing/overview`'s Free-fallback + real-subscription + 403 cases, `GET /v1/plans`'s public access + ordering + `selfServe` derivation), full suite 286/286 passing (`apps/api`+`packages/policy-engine`), `apps/dashboard` `tsc --noEmit` and full `next build` both clean (`/dashboard/billing` compiles into the build output at 2.22 kB). **Not verified**: the actual Paddle.js overlay call has not been exercised against a real Paddle sandbox account (no credentials in this environment) — same standing caveat as item 2's backend client; worth a real smoke test together once sandbox credentials exist. **Not built, per the item's own parenthetical**: no payment-method/invoice-history UI of our own — Paddle hosts both as merchant of record, so the screen just says so. **Checklist**: items 1-5 done — Phase 1's core productization loop (schema → Paddle checkout/webhook → usage metering → plan-limit enforcement → billing UI) is now complete end to end, at least as far as this environment can verify without live Paddle credentials. Remaining items (6 model discovery, 7 analytics scoping audit, 8 pricing page) are lower-priority and explicitly unordered relative to each other in the checklist; the exit-criteria Playwright test is still unwritten.
- **2026-07-24**: Shipped item 6, model discovery & sync. **Real near-miss caught before it happened**: went to create `apps/api/src/routes/models.ts` and the Write tool refused — the file already existed, registering the unrelated OpenAI-compat `GET /v1/models` endpoint (used by API clients listing available models). Read it first rather than overwriting, and extended its existing `registerModelRoutes()`/`ModelRouteDependencies` with the new discovery routes instead of creating a naming collision or a second registration function — the existing route and its `app.ts` call site are untouched apart from three new dependency fields. Added `ModelCatalog` (platform-level, no `workspaceId`, same category as `Plan`) and `infrastructure/model-discovery-client.ts` (`LiveModelDiscoveryClient`, native fetch + `AbortController`, no SDK — matching `paddle-client.ts`'s established style): OpenAI and Groq share one OpenAI-compatible list-models implementation, Anthropic and Gemini each their own, since their response shapes genuinely differ. `POST /v1/workspaces/:id/models/discover` reuses the pre-existing `models.manage` permission (Admin+Owner) rather than adding a new one — it was already the exact right fit. In `PROVIDER_MODE=mock` (this environment's default) no credential is touched at all; discovery reads the static `modelRegistry` and reuses its existing hand-curated capability fields (`supportsCode`, `costTier`, etc.) for `ModelCatalog.capabilitiesJson`, rather than leaving it empty — real provider list-models APIs return little beyond a model id (the pricing field stays `{}` always, matching the architecture review's own predicted "you'll need a hand-curated pricing table" pitfall). New `GET /v1/models/catalog` (public, same category as `GET /v1/plans`). **Second infrastructure gap found and closed within scope, mirroring item 5's pattern**: `/dashboard/models` was a pure demo-data stub (confirmed via grep — nothing else references it after this slice's replacement), so "add a discover button to the existing model-access screen" assumed a wired screen that didn't exist; rebuilt it for real (provider discover buttons + a live catalog table), but deliberately did NOT build enable/disable toggle UI — the actual model-access mutation route lives in `onboarding.ts` and is still `userId`-scoped, not workspace-scoped (a real, separate, pre-existing gap, flagged here rather than silently fixed mid-slice on an unrelated item). **Two dead-code findings, reported not fixed, per the "don't fix inline unless it blocks the current item" rule**: (1) `packages/providers/src/index.ts` declares an `LlmProviderAdapter` interface with its own `listModels()` method that looks like exactly what this task needed — grepped and confirmed zero consumers anywhere in the repo; it's vestigial, superseded by the actually-used `ProviderAdapter` interface, and this slice's new discovery client deliberately does not extend it. (2) `dashboard-view.tsx`'s `"models"` demo-data case is now unreachable — nothing calls `<DashboardView page="models" />` anymore after replacing the stub — but the same switch statement still serves other still-stub pages (`/dashboard/providers`, `/dashboard/costs`, etc.), so the file itself was left untouched. **Verified**: 7 new tests (`models.test.ts`: mock-mode discovery + idempotency + RBAC + cross-workspace rejection, live-mode credential decrypt-and-call via an injected fake `ModelDiscoveryClient` — a real `encryptCredential`/`decryptCredential` round trip, not a mock of the crypto itself — plus a missing-credential 404, and the public catalog GET), full suite 293/293 passing, `apps/dashboard` `tsc --noEmit` and full `next build` both clean (`/dashboard/models` now compiles at 1.62 kB, down from the ~241 kB shared demo-dashboard bundle other still-stub pages pull in). **Not live-verified**: the four real provider list-models calls have not been exercised against real credentials in this environment, same standing caveat as items 2 and 5. **Checklist**: items 1-6 done; items 7 (analytics scoping audit) and 8 (pricing page) remain, unordered relative to each other; the exit-criteria Playwright test is still unwritten.
- **2026-07-24**: Shipped item 7, workspace/API-key scoped analytics. **This was not the "confirm it's already covered" no-op the item's own wording hedged toward** — direct reads (not just an explorer report, given the severity) of `apps/api/src/routes/analytics.ts`, `resilience.ts`, `app.ts`'s registration call, `analytics-service.ts`'s SQL filtering, `app-shell.tsx`'s nav-gating comment, and `apps/dashboard/src/lib/api.ts` confirmed a real, live, confirmed cross-tenant confidentiality gap: all 7 `/v1/analytics/*` routes and `GET /v1/resilience/provider-attempts` had **zero** authentication of any kind and accepted `workspaceId`/`userId` as fully unvalidated optional query params — any caller, with no API key at all, could read any workspace's cost/request/model/error/provider-attempt data simply by passing a different id in the query string. The service layer itself was never the problem — `PrismaAnalyticsService`'s raw SQL and `InMemoryAnalyticsService` both already filtered correctly by `workspaceId` whenever one was supplied; the entire gap was the route layer never requiring one. This is the same vulnerability class as the `onboarding.ts` IDOR closed 2026-07-20, one tier less severe (read-only aggregate data, not write access to credentials/keys) but genuinely live in the same sense — reachable on any deployed instance of this code, not a theoretical gap. **Fix**: migrated all 8 routes to `/v1/workspaces/:workspaceId/analytics/*` and `/v1/workspaces/:workspaceId/resilience/provider-attempts`, gated with the pre-existing `analytics.read` permission (already granted to every role including Viewer — no new permission needed, no CRUD to build) plus `ensureSameWorkspace`; `workspaceId` is now always derived from the validated path param, never the query string, which also makes the secondary `userId` filter safe-by-construction (any WHERE clause is now always additionally bounded by the caller's own workspace). `/v1/resilience/circuit-breakers*` and `/v1/health/*` were deliberately left untouched — they're platform-wide operational state with no tenant dimension to violate; gating them "for consistency" would have been cosmetic, not a real fix, and the item's own wording is scoped to "analytics." Dashboard's `useDashboardData()` (`apps/dashboard/src/lib/api.ts`) now resolves the active workspace via the existing `getActiveWorkspace()` helper and calls the new scoped paths with `credentials: "include"` — it previously sent zero auth and displayed platform-wide aggregate data to every viewer regardless of role or workspace, a direct symptom of the same gap rather than real per-tenant reporting despite hitting genuinely real (non-stub) endpoints; falls back to the existing demo-data path with a clear message if no active workspace is set. **Two additional real consumers found and fixed, not deferred, since leaving them would have been a compile-breaking regression rather than a dormant gap**: `@routemind/sdk`'s public `analytics.summary()` and `@routemind/cli`'s `analytics`/`costs` commands both called the now-removed unscoped route with no `workspaceId` at all — the SDK method gained a required `workspaceId` first parameter (`analytics.summary(workspaceId, filters?, options?)`), and the CLI's two commands gained a `--workspace <workspaceId>` required option; both were genuinely broken by this fix otherwise, not just stale docs. **Flagged, not fixed here — genuinely out of this item's scope, added as item 9**: `apps/api/src/routes/evaluations.ts` has the identical unscoped-route shape (grepped: zero `workspaceId` references in the entire file) and is reached by the same `useDashboardData()` bundle — a live, structurally-identical gap, deliberately not absorbed into this slice per the standing "surface new findings as their own item" practice. **Also removed**: one now-obsolete test in `workspaces.test.ts` that exercised the just-removed unscoped route directly with a fake `workspaceId` string — its concern is now covered far more rigorously in `analytics.test.ts`'s dedicated cross-workspace isolation test. **Verified**: 15 new integration tests (11 in `analytics.test.ts`, 4 in a new `resilience.test.ts`) covering per-route success, date/userId filtering, unauthenticated 401, cross-workspace-key 403, Viewer-role access, and a dedicated isolation test proving one workspace's requests never appear in another's summary even when both share the same underlying log store; `createPrismaWorkspaceTestApp` (shared RBAC fixture) extended with optional in-memory log-store overrides to make this provable without a full Postgres-backed test. Full `apps/api` suite 288/288 passing (one pre-existing test needed updating to the new route shape, described above — not a regression), `packages/sdk` 11/11 passing, `packages/cli` 6/6 passing, full monorepo `tsc -b` build clean, `apps/dashboard` `next build` clean, lint clean across every touched package. **Not done, correctly out of scope**: `evaluations.ts` (item 9); the exit-criteria Playwright test is still unwritten. **Checklist**: items 1-7 done; items 8 (pricing page) and 9 (evaluations scoping, newly added) remain, unordered relative to each other.
- **2026-07-24**: Shipped item 8, the public pricing page. Added `Pricing` (`apps/website/src/components/landing/pricing.tsx`), a client component fetching the public `GET /v1/plans` at render time with no hardcoded plan numbers, wired into the marketing site's single-page layout (`page.tsx`, between Features and the Playground) plus a new "Pricing" navbar link — neither a `/pricing` dashboard route nor any pricing content existed anywhere before this. **Two real bugs found and fixed via live browser testing, not caught by `next build` alone**: (1) the first draft used `plan.selfServe` (`paddlePriceId !== null`) to decide "Contact sales" — since no real Paddle price IDs exist in this environment (items 2/5's standing sandbox caveat), every plan's `selfServe` is `false` today, which would have shown "Contact sales" for Free/Pro/Team too, directly violating the item's explicit requirement; fixed by deriving it from `featuresJson.contactSales === true` instead, the actual signal for "this plan has no fixed public price." (2) `GET /v1/plans` only orders by `priceCents` ascending, and Enterprise's `priceCents` is `0` (tied with Free's) — a live fetch showed Enterprise rendering first; fixed with a client-side re-sort scoped to this component (contact-sales plans always last), leaving the shared route's ordering untouched for other consumers like the dashboard billing screen. **Verified live**: ran the API (mock mode, real seeded `Plan` rows) and website dev servers locally, drove a real Chromium browser to `/#pricing` via Playwright, and screenshotted all four cards in the correct order with correct prices, request limits, feature checklists, and CTA labels; zero console errors. `apps/website` build and prettier clean; no automated test suite exists for this app (confirmed via glob, pre-existing gap). **Not done**: no automated (Playwright/Vitest) regression test for this page — verification was a one-time manual browser check, matching this app's existing lack of any test infrastructure. **Checklist**: items 1-8 done; item 9 (evaluations scoping, added during item 7's audit) is the only remaining checklist item; the exit-criteria Playwright test is still unwritten.
- **2026-07-25**: Shipped item 9, workspace-scoping `/v1/evaluations/*`. **Investigated before assuming the item's own premise held**: its wording said "apply the identical fix pattern" as item 7, implying a pure route-layer fix — a fresh explorer pass plus direct reads found that wasn't true: `EvaluationDataset`/`EvaluationCase`/`EvaluationRun` have no `workspaceId` column in `schema.prisma` at all, unlike `RequestLog`/`ProviderAttemptLog` (which already had one for item 7). That looked like it made this item 2-3x bigger (a schema migration + interface refactor), until a deeper check found the schema tables are a red herring: **the entire evaluations feature is served by `InMemoryEvaluationService` in every environment including production** — confirmed via grep, no `PrismaEvaluationService` exists anywhere, so no schema migration was actually needed; the fix was interface + in-memory-store + route changes only. Every `EvaluationService` method now takes `workspaceId`; migrated **all 9** evaluation routes (not just the 3 the item named — the other 6, covering dataset/case/run creation and detail lookups, had the identical unauthenticated shape and would have stayed exploitable if left alone) to `/v1/workspaces/:workspaceId/evaluations/...`, gated with the existing `analytics.read` permission (reused, no new permission) plus `ensureSameWorkspace`; cross-workspace dataset/run access returns an identical 404, mirroring the provider-credentials ownership-can't-be-probed pattern rather than leaking existence via a 403. **A second real bug found in the same investigation, fixed as part of this item since it's the same interface change**: `evaluationService.routingScores()` runs on every `/v1/chat/completions` request (hot path, influences model selection) and was being called with zero workspace scoping — one workspace's evaluation runs were silently shaping every other workspace's routing decisions. `routingScores(workspaceId?)` now receives the caller's own `user.workspaceId`; the legacy dev-key path (predates workspaces) gets `[]` instead of a cross-tenant blend. `createPrismaWorkspaceTestApp` (shared RBAC fixture) extended with `evaluationService`/`routerLLMServiceFactory` overrides so this could be tested without hand-rolling a new fixture. Rewrote `evaluations.test.ts` fully onto the Prisma-backed RBAC pattern: all four pre-existing scoring/routing tests preserved with workspace context threaded through, plus new unauthenticated-401, cross-workspace-403, and a dedicated isolation test (workspace B sees zero of workspace A's datasets/scores; a direct cross-workspace dataset-cases lookup 404s) — 8/8 passing. Dashboard's 3 evaluations calls in `useDashboardData()` updated to the workspace-scoped paths, same as item 7; no SDK/CLI consumer exists for evaluations (grepped, confirmed), so no external-consumer blast radius this time. **Verified**: full `apps/api` + `packages/routing` suite 305/305 passing, `apps/dashboard` build clean, lint clean across every touched file. **Flagged, not fixed here — added as item 10, a materially bigger undertaking than "scope the routes"**: `apps/api/src/scripts/seed-dev.ts` writes real rows into the `EvaluationDataset`/`EvaluationCase` Postgres tables via raw SQL, but nothing in the running application ever reads them — evaluations have zero real persistence in any environment, including production, and every server restart silently wipes all evaluation data. **Checklist**: items 1-9 done; item 10 (evaluations persistence gap) is the only remaining item; the exit-criteria Playwright test is still unwritten.
- **2026-07-25**: Shipped item 10. This item was written as an explicit either/or ("build real persistence, or remove the dead schema") — asked before picking a side rather than defaulting silently, since the two paths have very different scope and different implications for whether Evaluations stays a real product feature; chose removal. Confirmed via a dedicated investigation (not just the write-up from item 9) that the four `Evaluation*` Prisma models are genuinely dead: zero `prisma.evaluationDataset`/`evaluationCase`/`evaluationRun`/`evaluationResult` calls anywhere in the repo, no back-relations from any other model, not re-exported from `packages/shared/src/types`. Removed all four models from `schema.prisma` (71 lines, pure deletion — confirmed via `git diff --stat`) and `seed-dev.ts`'s dead `seedEvaluationDatasets()` function plus its call site, which only ever wrote rows nothing read back. New migration `20260724191828_drop_unused_evaluation_tables` drops exactly those 4 tables and their FKs, nothing else. **Gotcha, same recurring class as prior sessions**: `prisma generate` hit the familiar Windows `EPERM` on the query-engine DLL — this time traced to two leftover `npm run dev -w apps/api` process trees from earlier browser-testing sessions that were never fully stopped (confirmed via `Get-CimInstance Win32_Process` since `wmic` isn't available in this Bash environment); killing them (not just retrying) resolved it — worth checking for stray dev-server processes first if this recurs again, rather than only retrying. **Verified**: `npm run seed:dev` completes cleanly with no evaluation-table writes attempted (confirming the removal didn't break the one thing that used to touch these tables); full `apps/api` suite 291/291 passing, unchanged from before the removal (confirming zero behavioral impact on the actual, working, in-memory evaluations feature); full monorepo `tsc -b` build clean; lint clean. **Checklist**: items 1-10 done — Phase 1's checklist is now fully complete. **Not done**: the exit-criteria Playwright test (sign up → hit Free tier's limit → upgrade prompt → real test-mode payment → limit lifted, end to end) is still unwritten — the last thing standing between Phase 1 and being declared done, per the phase plan's own exit-criteria section.
- **2026-08-16**: Deployed Phase 1's accumulated schema work to production for the first time — items 1, 2, 6, and 10's migrations had only ever been applied to local dev until now. `prisma migrate status` against the production Neon DB showed 4 of 25 migrations pending: `add_plan_subscription`, `paddle_billing_fields`, `add_model_catalog`, `drop_unused_evaluation_tables`. Read all four `migration.sql` files before applying anything (matching this project's standing db-migration-check discipline): the first three are pure additive `CREATE TABLE`s; the Paddle-fields migration renames `Subscription.stripeSubscriptionId`, but since `Subscription` itself didn't exist in production before this same deploy, there was no real data at risk; the evaluation-tables drop matches this session's own exhaustive investigation (item 10) that those four tables had zero real application consumers. `prisma migrate deploy` applied all four cleanly; `prisma migrate status` re-run afterward confirmed "Database schema is up to date!" (25/25). **Follow-up gap found and closed in the same pass**: the newly-created `Plan` table was empty (0 rows) in production — `GET /v1/plans`, the public pricing page, and plan-limit enforcement's Free-tier fallback would all have been broken until seeded. Ran `seed:plans` against production; verified all four plans (Free/Pro/Team/Enterprise) landed with the same real pricing already live on the pricing page. **Confirmed, not assumed**: every plan's `paddlePriceId` is `null` in production, same as local dev — no live Paddle sandbox price IDs are configured there either, so the pricing page will correctly show real prices for Free/Pro/Team (driven by `featuresJson.contactSales`, not `selfServe`, per item 8's fix) but the actual Paddle checkout flow will 503 in production too until real `PADDLE_API_KEY`/`PADDLE_CLIENT_TOKEN`/`PADDLE_WEBHOOK_SECRET` and real `Plan.paddlePriceId` values are configured — a business/ops task, not something touched here. **Flagging, not fixing, same recurring pattern as prior sessions**: the user pasted the live production Neon connection string (including password) directly into chat to enable this — the credential is now in conversation history outside anyone's control and should be rotated, independent of this work being otherwise complete and correct.
- **2026-07-25**: Shipped the Phase 1 exit-criteria test (`apps/api/src/routes/phase1-exit-criteria.test.ts`), closing the last item standing between Phase 1 and done. **Same standard as Phase 0's own exit-criteria test, not a stricter one**: re-read `phase0-isolation.test.ts` first and confirmed it's a Vitest integration test driving real routes via `app.inject()` against real Postgres, not a browser-driven Playwright test — the phase plan's "Playwright/integration test" wording is an either/or, and matching Phase 0's own precedent was the more consistent choice. One test proves the full loop against one real Postgres-backed org: real magic-link signup (`RecordingEmailSender` + token extraction, same pattern as `auth.test.ts`), a real self-issued API key (`POST /v1/workspaces/:id/api-keys` called with the session cookie signup itself grants — no support ticket, no manual provisioning), the real seeded Free plan's actual 10,000-request limit (bulk-inserted via `requestLog.createMany` rather than 10,000 real HTTP round trips, for speed — data-equivalent, not a shortcut on what's being tested), a real `403 PLAN_LIMIT_EXCEEDED` block, the real `GET /v1/plans`/`billing/overview` data an upgrade prompt would render from, a real checkout call against the real seeded Pro plan, and a real, correctly HMAC-signed `POST /v1/webhooks/paddle` delivery — then proves the very next request succeeds under Pro's limit, same running app instance, no restart. **The one honest gap, stated plainly rather than glossed over**: no live Paddle sandbox credentials exist in this environment (items 2/5's standing caveat), so a real browser driving Paddle's actual hosted checkout UI isn't possible here — "pay with a real credit card" is represented by simulating the one external event Paddle itself sends on a real payment (a signed `subscription.created` webhook), which is genuinely how this backend learns about payment outcomes in production too, since it never sees a card number either way. **The only thing faked, and only for the duration of one test**: the real seeded "Pro" plan's `paddlePriceId` is temporarily set to a test value (since it's `null` in this environment, same as every plan), so checkout can run against the real Pro name/price/limit rather than a synthetic stand-in plan — restored to `null` in `afterEach` regardless of test outcome, mirroring the exact same temporary-mutate-and-restore pattern `cost-guardrail-policy.test.ts` already used for Free's `includedRequests`. **Real infrastructure gap hit and fixed along the way, not a test bug**: the local Postgres/Redis Docker containers had stopped (Docker Desktop itself wasn't running) — started Docker Desktop, then `docker start llm-router-postgres-1 llm-router-redis-1`; unrelated to any code in this repo, just this machine's state. `createPrismaWorkspaceTestApp` (shared RBAC fixture) extended once more with an `emailSender` override, the same repeatedly-used pattern from items 7 and 9, so one app instance could do real signup, real chat completions, and real billing/webhooks together instead of needing three separate fixtures. **Verified**: new test passes in ~1.9s standalone; full `apps/api` suite 292/292 passing (291 previous + 1 new); full monorepo `tsc -b` build clean; lint and prettier clean; cleanup independently confirmed via a direct query (Pro plan's `paddlePriceId` back to `null`, zero orphaned `exit-criteria-*` users). **Phase 1 is now fully done**: all 10 checklist items plus the exit-criteria test are complete — ready to move to Phase 2 on the next `/phase-next` run, pending a decision on whether Phase 2 needs its own plan file written first (it doesn't have a `docs/phase-2-plan.md` yet, only the high-level Phase 2 section in this roadmap).

---

## Phase 2 — Trust & Differentiation

**Theme: make the USPs from the market research real and provable, not aspirational**
**Timeframe: ~8–10 weeks | Engineering + first design partners**

This is where you stop being "another gateway" and start being _the_ gateway with a specific, defensible claim. Everything here maps directly to USP 1 (BYO Everything) and USP 3 (transparent pricing) from the market research.

**Ship:**

- Harden BYO Router Model: explicit, surfaced fallback behavior (`routemind.routing.reason` in every response) so degraded routing is never silent
- Data retention / prompt-logging controls, configurable per workspace — this is both a trust feature and table stakes for your next regulated-industry conversation
- Rate limiting distinct from budgets (protects you, not just the customer)
- A genuinely honest pricing page: no "recorded logs" ambiguity, no per-service licensing surprises — this is the direct competitive jab at Portkey and Kong's most-criticized pain point
- 3–5 design partners, ideally teams who've been burned by a Portkey invoice or a Kong contract — recruit through the exact language of USP 1/USP 3, not generic outreach

**Business/GTM work starts here, not before:**

- One-page positioning doc (the statement from the market research) turned into an actual landing page
- Case study or two from design partners, focused on the "predictable bill" and "we never see your keys" claims specifically — proof, not adjectives
- Start watching Bifrost's governance roadmap on a monthly cadence; that's the one competitor whose trajectory could occupy this same seat before you

**Exit criteria:** you can point to a real (even if small) customer paying real money, on a plan whose price they predicted correctly before the invoice arrived, using BYO router credentials — i.e., every clause in your positioning statement is demonstrably true for at least one real workspace, not just true in the codebase.

**Why this order:** USPs are marketing claims until someone outside your team relies on them. Phase 2 is the phase where "differentiator" changes from a slide in a review doc to a thing a paying customer would vouch for.

### Session log

- **2026-08-16**: Created `docs/phase-2-plan.md`, converting this section's Ship list into a concrete checklist, and flipped this file's "Current phase" line from Phase 1 to Phase 2. **Investigated before writing a single checklist item, per standing practice**: two of the four Ship-list lines turned out to already be fully shipped as a side effect of earlier phases' work, verified directly (not just via an explorer report) rather than assumed — checked off immediately in `phase-2-plan.md` with the evidence, not silently dropped from the list. (1) "Harden BYO Router Model: explicit, surfaced fallback behavior" — `routemind.routing.reason`/`routingMetadata.configSource` are populated on every `/v1/chat/completions` response, with four distinct, exported fallback-reason constants in `packages/routing/src/index.ts`, all landed 2026-07-19 during Phase 0's evaluations work, unrelated to Phase 2 being declared at the time. (2) "Rate limiting distinct from budgets" — `apps/api/src/infrastructure/rate-limiter.ts` is genuinely live-wired into `/v1/chat/completions` (runs before cost-guardrail checks, real `429`s, real `x-ratelimit-*` headers) with a hardcoded 100 req/hour per API key; this satisfied the roadmap's literal wording ("distinct from budgets") even though the limit isn't yet configurable per plan tier — flagged as a known limitation, not treated as blocking. **Real, confirmed gap for the two remaining checklist items**: `LLMResponseCache` persists the full `promptText` and `responseJson` of every cached request in Postgres indefinitely, with zero workspace-level control — no retention-policy table, no opt-out, nothing (`RequestLog` itself only ever stored metadata, not content, so the cache table is where the actual sensitive data lives). This is item 3 on the new checklist, the first genuinely unchecked item. Item 4 (honest pricing/trust-page messaging about data handling) is ordered after it, since it can't honestly describe a toggle that doesn't exist yet. The GTM/business bullets (design partners, case studies, competitor watching) were kept out of the numbered checklist entirely and moved to their own non-code section, matching the roadmap's own Ship/Business-GTM separation — `/phase-next` runs code slices, and those items aren't code. **Not done**: item 3 itself — this session log entry is phase-transition bookkeeping, not a shipped slice; the actual first `/phase-next` implementation slice for Phase 2 starts next.

---

## Phase 3 — Scale & Observability

**Theme: survive real production traffic without you personally watching it**
**Timeframe: ~8–10 weeks | Infrastructure-heavy**

This is the v3.0 infrastructure maturity work — deliberately _after_ Phase 2, not before, because scaling infrastructure nobody is using yet is wasted engineering. You earn the right to build this phase by having Phase 2's design partners generate real load.

**Ship:**

- Logical control-plane/data-plane separation (split connection pools, add read replica) — physical separation only if traffic genuinely demands it
- OpenTelemetry + Prometheus/Grafana, with routing as its own span
- Langfuse integration, gated by the same per-workspace data-retention controls from Phase 2 (don't undercut your own trust story)
- On-call basics: alerting on error rate, latency, budget-breach, circuit-breaker trips — this is the unglamorous work that actually earns "production-grade"

**Exit criteria:** a design partner's traffic spikes 10x for a day and you find out from a Grafana dashboard, not from their support email.

---

## Phase 4 — Platform Expansion

**Theme: earn the right to more surface area**
**Timeframe: ongoing, feature-by-feature | Only after Phase 3 is stable**

This is Proposed Feature 9, deliberately deprioritized until now, and deliberately selective rather than "build all six." Per the earlier roadmap: shadow routing and prompt versioning first, because shadow routing is a direct extension of the BYO-router trust story (customers can validate a new router model against real traffic without risking cost or quality), and versioning is the prerequisite everything else in this bucket needs.

**Ship (in this order):**

1. Prompt versioning (prerequisite for the rest)
2. Shadow routing (extends USP 1 — "test a routing change with your own credentials, at your own risk tolerance, with zero blast radius")
3. MCP gateway — only now, once it's clear the market has fully standardized on it as table stakes (it's trending that way per the research, but wasn't worth building shallow in Phase 0–2)
4. Prompt playground / model comparison — nice-to-have, build only if design partners specifically ask
5. Prompt library / evaluations — lowest priority of the six; revisit only with clear demand signal

**Exit criteria:** each shipped feature has at least one design partner who requested it by name before you built it — this phase is the one most at risk of feature-bloat for its own sake, so demand-gating matters more here than anywhere else in the plan.

---

## Phase 5 — Enterprise Readiness

**Theme: the features that unlock the deals you can't close today**
**Timeframe: as revenue and demand justify it — this is a "when triggered," not "by date," phase**

**Trigger condition:** you have a real Enterprise-tier prospect asking for these specifically. Building them speculatively means competing with Kong/TrueFoundry's sales-led motion on their terms, which you can't win pre-revenue.

**Ship (only when triggered):**

- SSO/SAML for the dashboard
- SOC2/compliance certification process (this is a 6+ month organizational commitment, not a sprint — start the process the moment you have a prospect who needs it, since the clock runs in parallel with sales)
- Dedicated/VPC/self-hosted deployment options
- Custom SLAs and contract billing (vs. self-serve Stripe)

---

## The plan as a single picture

| Phase                       | Question it answers                                 | Depends on                           | Primary risk if skipped or reordered                                                                           |
| --------------------------- | --------------------------------------------------- | ------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| 0 — Foundation              | Can two tenants safely share this system?           | Nothing (start here)                 | Every later phase requires re-doing schema work                                                                |
| 1 — Productization          | Can a stranger pay you without talking to you?      | Phase 0                              | USPs stay theoretical; no real usage data to build on                                                          |
| 2 — Trust & Differentiation | Is your USP actually true for a real customer?      | Phase 1                              | You compete on features instead of your one real edge; Bifrost or another entrant claims the positioning first |
| 3 — Scale & Observability   | Does it survive traffic you don't personally watch? | Phase 2 (needs real load to justify) | Wasted engineering on infra for traffic that doesn't exist yet                                                 |
| 4 — Platform Expansion      | Does the platform grow with real demand?            | Phase 3                              | Feature-bloat without customers who asked for it                                                               |
| 5 — Enterprise Readiness    | Can you close the deals that need this?             | A real trigger event                 | Months of compliance/SSO work with no prospect waiting on it                                                   |

---

## How I'd actually think about this if it were mine

The single biggest risk to this plan isn't any individual phase — it's the pull to jump to Phase 4 (the fun, differentiated AI-platform features) before Phase 0–1 are actually done, because that's the part that feels like "real product work." Almost every technically strong solo/small-team project I'd compare this to fails at exactly that transition: the founder-engineer keeps making the core more sophisticated because that's where the skill and interest is, while the boring productization work (billing, self-serve onboarding, a pricing page that doesn't say "contact sales") — the part that actually makes it a _product_ — keeps getting pushed to "next sprint."

Given that RouteMind is currently a portfolio/side project rather than a funded company, I'd also flag a sequencing question worth deciding explicitly rather than by default: are you building this toward becoming an actual company with paying customers, or is the primary goal a demonstrably strong AI-infra portfolio piece for the interview process you mentioned? Both are legitimate goals, but they change the plan — if it's the latter, Phase 0 plus a strong Phase 2 (real design partners, even 1–2 unpaid ones, plus honest metrics) is probably enough depth to be compelling in an interview, and Phases 3–5 are lower-priority polish. If it's the former, Phase 1's billing and self-serve work can't be skipped or softened, because that's the phase that turns "impressive project" into "business."
